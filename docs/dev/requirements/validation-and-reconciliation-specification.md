# Validation and Reconciliation Specification

> **Status**: Draft · **Date**: 2026-09-12
>
> **Decision state**: 对账口径、差异分类、重跑验证与门禁语义已确定（§2 至 §5），R1 与 R2 已在
> L-1 与分配层范围内具体化（§2.3、§2B.3），L-1 的状态机违规注入已列出（§6.1），
> **结算段的业务校验层见 §2A，分配与确认段的断言见 §2B，资金段的业务校验层见 §2C**。
> **S-3 与 S-4 两个撤销场景已于 2026-09-12 冻结**，相应检查见 §2A.5.1。
> **四段的故障注入清单已于 2026-09-12 补齐（§6.2 至 §6.6）**，
> 每条带档位、预期捕获者、预期结果与失效场景。
> 具体阈值、容差数字与注入比例**待实测**，见 §7。
>
> **2026-09-12 改标**：读完 ISO 20022 MDR Part 2 后，§2A.3 的三处"依据"由 CMOP 改为规范，
> 见[监管与行业数据契约](regulatory-data-contracts.md) §4C.1。**检查本身一条未改，改的是归属。**
>
> **Related**: [业务目标](business-objectives.md)、[合成数据生成规范](data-generation-specification.md)、
> [工作负载基线](../workload-baseline.md)

## 1. 本文的作用与边界

本文规定**怎样判断一次运行的结果是对的**。它服务于主 BO：对账不平时能定位到笔，而不是
只报告总数不一致。

它不规定生成什么，那属于[生成规范](data-generation-specification.md)；也不规定怎么实现
检查，那属于 design。

## 1A. 本层的四张分类表，2026-09-12 建立

**建立的理由是同一天里三样东西各自增长到第五、第四、第九条**（第四张表 §1A.4 于 2026-09-13 补），
分散在各节里数着"这是第几条"已经不可靠。**此后新增检查一律先归类，再落节。**

### 1A.1 检查的形态：五种

| 形态 | 判的是什么 | 实例 |
|---|---|---|
| 一、等式 | 两个数相等或相差在容差内 | R1、R2、P4-1、C1-3 |
| 二、引用链可达性 | 一个标识能否命中另一处 | VA-11、VC-10、C4-1 |
| 三、状态停留时长 | 一个状态停了多久 | C-3、C4-7 |
| 四、**结构存在性** | 一条路径上每一层在不在 | **C4-19、V2-13** |
| 五、**词表成员资格** | 取值在不在某个码集里 | **V2-11、V2-12、E-8**，及 §2C.7 全节 |

**形态四与形态五都是 2026-09-12 加入的**，此前只有前三种。
**两者的共同点是：它们不看值对不对，看值合不合法。**
**形态五必须带码集名与词表版本**，见 §2C.7 第三与第五句。

### 1A.2 适用条件的来源：四种，且第四种要小心

| 来源 | 条件在哪 | 实例 |
|---|---|---|
| 一、无条件 | — | P4-1 |
| 二、被判报文自身的字段 | 同一张报文 | V2-3（`PrtlSttlmInd`）、C1-9（`GrpRtr`） |
| 三、同一链上的另一张报文 | 同 `TxId` 或同 `EndToEndId` | C4-5、C4-7 |
| 四、**第三张报文，且它可以不存在** | 别的链、别的段 | **C4-17（条件是 `pacs.002` 的 `TxSts`）** |

**第四种的危险在于"那张报文不存在"有两种含义**，而数据上无法区分。
**凡用第四种，必须同时有一条检查负责判定"该报文是不是本项目自己漏发了"**，
见 §2C.6 里 C4-26 与 C3-3 的配对说明。

### 1A.4 依据的强度：四档，should 级指引统一处理，2026-09-13

**此前"依据"一列的写法各节不一**：规范、Guideline 级、CMOP、码值定义混着写。
**统一为四档，每条检查的结果行必须带这个字段**，与 §5 的三分类并列，不是替代。

| 档 | 来源 | 结果为 fail 时对外怎么说 |
|---|---|---|
| 一、**rule** | XSD 基数、具名 Rule 约束 | 报文违反规范 |
| 二、**definition** | 外部词表的码值定义文字 | 报文违反码值语义 |
| 三、**guideline** | 具名 Guideline 约束、码值定义里的 should 句 | **CMOP 生成偏离了规范建议**，不说违反规范 |
| 四、**CMOP** | 本项目口径 | CMOP 生成偏离了本项目口径 |

**第三档的处理办法只有两种，按"CMOP 是否生成被指引的情形"二选一，不存在第三种：**

1. **生成侧选择满足，并写检查。** 检查结果按第三档报告。实例：C1-13（`NbOfTxs` 等于原批笔数）、
   V2-15（`PAIN`…`PARC` 次序，C30／C32）、E-23（选项编号唯一，通知侧 C88）。
   **E-23 在指令侧有 `seev.033` C20 作 rule**，取强的一档：它按第一档报告。
2. **生成侧不生成被指引的码或路径，不写检查。** 实例：`SCEX`（契约 §4D.11.1）、
   `ExternalCorporateActionEventType1Code` 的 `OTHR`（契约 §4B.15.6）、
   `ACSC` 的双边协议限制（契约 §4B.15.3）、`ChngInstrInd` 的 C6（契约 §4D.12.5，登记为 S-8a）。
   **不生成必须写进生成规范的硬约束**，否则第二种就退化成"忘了"。

**不允许的第三种写法：生成被指引的情形，同时写一条检查去判它。**
should 不是 must，**判据既不能判它违规也不能判它合规**，只能永远报"不确定"，
而 §5 没有给"不确定"留位置。

**第二种的注入口径**：不配注入。被排除的码进入正常样本时，
**由词表成员资格检查（形态五）按"CMOP 生成子集"捕获**，不是由 should 句本身捕获。

### 1A.3 否定式断言：二十一条

**否定式断言要挡的不是坏数据，是一个看上去合理的写法。**
**它们的共同弱点是：在没有配对注入时，看起来永远是通过的。**

| 编号 | 挡的那个"合理写法" | 依据 |
|---|---|---|
| VA-6 | 分配数量等于成交总量 | **规范明确否定** |
| VA-12 | Replace 或 Cancel 之后必然被接受 | **规范明确否定** |
| C4-16 | 整组退汇能定位到笔 | 规范结构上不支持 |
| C4-18 | 日终对账单可以带未入账明细 | 报文 scope 禁止，**无约束支撑** |
| C4-20 | 未入账与已入账的入账日应当相等 | 同一元素两种语义 |
| C4-27 | 被拒的笔仍可能以某种状态入账 | CMOP |
| C4-28 | 被拒的笔应当有冲正明细 | **规范明确否定**（2026-09-12 由 CMOP 升级） |
| E-6 | 撤销之后该事件从未存在 | 审计要求留痕 |
| E-9／E-15 | 过账冲正应当回退调整因子 | **把两张报文的处理路径合并** |
| E-17 | 撤销之后总会有一条重发 | **规范区分两个码而不绑定后果** |
| E-21 | 终态报文上读得到撤销的真实原因 | **码集只剩两个值** |
| C4-35 | 任何状态的分录都能冲正 | **规范明确否定** |
| C4-36 | 已上账的分录可以退回未上账 | CMOP |
| C4-38 | 仅供参考的分录也该进余额 | **规范明确否定** |
| E-30 | 同一选项在通知、指令、确认上的 `OptnTp` 相等 | **三张报文的码集不同**（2026-09-13） |
| E-31 | 默认执行的状态回报应当回引一条指令 | 规范结构上不支持：`InstrId` 0..1，沉默者无指令可引 |
| V2-14 | 不允许部分结算的指令下也可能有部分确认 | **规范明确否定**：`NPAR` 的码值定义 |
| V2-19 | 拆分出的新指令各自的确认应当标部分结算 | CMOP |
| VA-16 | 重下的分配能沿字段回溯到被撤销的原件 | 规范结构上不支持：`RefAllocID` 的定义不覆盖 New |
| VC-15 | 重下的确认能沿字段回溯到被撤销的确认 | 规范结构上不支持：`ConfirmRefID` 同上 |
| VC-16 | Cancel 型确认必有应答，**或**必无应答 | **规范流程图不画且有粘贴痕迹**，双向 |

**二十一条按依据分：六条"规范明确否定"、五条"规范结构上不支持"、
一条"scope 禁止而无约束"、一条"码集不给"、一条"同一元素两种语义"、
一条"兄弟报文码集不同"、一条"流程图不画"，其余五条纯属 CMOP 口径。**
（2026-09-13 由十四条增至十八条，新增四条来自契约 §4D.12 与 §4D.13；
同日再增三条至二十一条，来自契约 §3A.11a。）

**这个分布在 2026-09-12 当天变过一次。** C4-28 原记为 CMOP，
**改判的原因不是重新想了想，是读到了另一份文件**：
外部词表里 `PDNG` 与 `BOOK` 的码值定义各有一句话直接否定它要挡的写法
（契约 §4B.14.3）。**由此定一条规矩：**

> **把一条判据标为 CMOP 之前，必须先查相关码值在外部词表里的定义文字。**
> **码值定义会带业务规则，而它既不在报文 XSD 里，也不在 MDR 里。**

**此前所有标 CMOP 的判据都要按这条重查一遍**，登记为未结事项。
**这个分布要写进对外口径**：说"本层拦住了 X"时，
**X 是规范禁止的还是本项目禁止的，是两句不同的话**。

**每一条否定式必须配一条"合法沉默"注入**，即制造一份符合该写法所禁止之情形的
合法数据，**并断言全部检查保持沉默**。

**E-21 值得单独看一眼。** 它挡的写法在别的段落一直成立：
从终态报文回读原因码，是本项目其他各段的标准做法。
**只有这一处，`CancelledStatusReason5Code` 把十一个原因压成两个**，
于是同一个做法在这里静默地给出错误答案。
**否定式最难发现的一类，就是"在别处正确"的写法。**

**这条规矩是 2026-09-12 补齐的，补齐之前有五条否定式从未被验证过**：
C4-16、C4-18、C4-27、C4-28、E-6／E-15。**它们此前一直显示为通过**，
而按本文自己的口径，那句"通过"没有任何内容。现已各配一条注入，见 §6.4A 与 §6.5。
**这件事本身要记住**：否定式在缺注入时的表现，与它被正确实现时的表现完全一样。

## 2. 对账口径

### 2.1 四个域与比对关系

| 比对 | 左侧 | 右侧 | 主要用途 |
|---|---|---|---|
| R1 | 成交事件 | 持仓变动 | 数量是否传导 |
| R2 | 成交事件 | 现金流水 | 金额与费用是否传导 |
| R3 | 内部持仓 | 清算方持仓 | 与外部是否一致 |
| R4 | 内部现金 | 对手方对账单 | 与外部是否一致 |

R1 与 R2 是内部一致性，R3 与 R4 是外部一致性。两类的差异含义完全不同：内部不平是自己的
处理错误，外部不平可能是对方的错误或口径差异。**不得混在同一个异常队列里不加区分。**

### 2.2 时点基准必须逐对声明

**这是最容易出错的地方。** T+1 结算意味着 T 日的成交在 T+1 日交割，现金比成交晚一天。
若 R2 用同一个日期同时切两侧，结果是**全部不平**，而且看上去像是系统坏了。

因此每一对比对必须显式声明左右两侧各自的日期基准：交易日、结算日、还是入账日。**没有声明
日期基准的比对不允许上线**，这条比任何阈值都重要。

### 2.3 L-1 范围内的 R1 与 R2 具体化

L-1 冻结后，两组内部一致性比对可以写实。字段依据见
[监管与行业数据契约](regulatory-data-contracts.md) §3.3 与 §3.6。

| | R1 成交对持仓 | R2 成交对现金 |
|---|---|---|
| 左侧取数 | `ExecType=F` 的成交回报，按 `ExecID` 去重 | 同左 |
| 参与字段 | `LastQty(32)`、`Side(54)`、标的与账户 | `LastQty(32)`、`LastPx(31)`、`Currency(15)` |
| 聚合式 | 按买卖方向对 `LastQty` 求和 | 对 `LastQty × LastPx` 求和，再加费用 |
| 左侧日期基准 | `TransactTime(60)` 所属交易日 | `TransactTime(60)` 所属交易日 |
| 右侧日期基准 | **取决于持仓口径，见下** | **结算日，等于交易日加一** |
| 容差 | 零，数量不设容差 | 待定，仅允许因舍入产生 |

**R1 必须先声明比的是哪一种持仓。** 交易日持仓在成交当日变动，结算日持仓在 T+1 变动。两者
都是正确的持仓，但对不上同一批成交。**不声明就比，等于随机挑一种然后把另一种全报成差异。**

**R2 的两侧日期天然错开一天。** 左侧按交易日切，右侧必须按结算日切，映射关系是 T 到 T+1。
这是 §2.2 那条规则最直接的例子：两侧用同一个日期切，结果是全部不平。

### 2.4 容差

- **容差按比对对声明，不设全局默认值。** 零容差的比对必须显式写成零，不能靠"没配置"来
  表示。
- **容差只允许用于因舍入产生的派生金额。** 数量类差异**没有容差**，差一股也是差异。
- 每个非零容差必须写明理由与推导，不能只写数字。

## 2A. 结算段的业务校验层

**本节回应[监管与行业数据契约](regulatory-data-contracts.md) §4.3 末尾提出的要求。**
它规定结算段在 schema 校验之外必须再过哪一层，**不规定实现，那属于 design**。

### 2A.1 为什么这一层不可省

**结算段的 schema 校验通过，几乎不说明任何业务正确性。** 三条已核实的事实：

1. `sese.023.001.13` 沿必填分支走下去，**真正必填的叶子只有七个**。
2. `FinInstrmId` 本身必填，但它的三个子元素 `ISIN`、`OthrId`、`Desc` **全部可选**。
   因此**一条只带空 `<FinInstrmId/>` 的结算指令是 schema 合法的**，
   即一条不说明结算什么证券的结算指令。
3. `sese.024.001.14` 顶层只有 `TxId` 必填，**四个状态元素全部可选**。
   一条什么状态都不带的状态回报同样合法。

**与 FIX 的差别要说清楚。** FIX 4.4 的必填列至少挡得住一部分空报文，
**ISO 20022 挡不住**。把"XSD 通过"当作结算段的验收依据，等于没有验收。

**这一层的产出不是差异，是契约违规。** 与 §6.1 里 L-1 的处理方式一致：
**未通过本层的报文与其所在链条不进入正常对账队列**，
否则它们会以"数量不一致"或"缺失"的形式污染 §3 的分类统计，
**让真实差异被自造的噪声淹没**。

### 2A.2 三级，按判定所需的信息范围划分

**分级依据是"判定这条检查需要看到多少东西"**，不是严重程度。
这样分是因为三级各自能在不同的处理阶段执行：第一级在解析单条报文时就能跑，
第三级要等整条链拼好。

| 级 | 判定范围 | 执行时机 |
|---|---|---|
| V1 | 单条报文内部 | Bronze 解析后立即 |
| V2 | 同一 `TxId` 的报文集合 | Silver 拼链条时 |
| V3 | 场景契约 | 链条拼完，且已知该链属于哪个场景 |

### 2A.3 V1：单条报文内部

**"依据"一列区分规范要求与 CMOP 决定，两者的可改动性完全不同。**
标 CMOP 的可以随场景扩展调整，标规范的不能。

**规范要求有两种，本表不再区分它们的出处但要在这里说清。** 一种写在 XSD 里，任何标准
XML 校验器都会执行；另一种是 MDR Part 2 的**具名约束**，形如"若 A 缺失则 B 必须存在"，
**XSD 表达不了，没有校验器会执行**。**后者的唯一执行者就是本层**，这是本层存在的第二个
理由——第一个理由（schema 挡不住空报文）在契约 §4.3，第二个理由在契约 §4C.1。

#### `sese.023` 结算指令

| 编号 | 检查 | 依据 | 适用条件 |
|---|---|---|---|
| V1-023-1 | `TxId` 非空 | 规范，1..1 | 全部 |
| V1-023-2 | `FinInstrmId` 至少有一个子元素非空，且走 `OthrId` 而非 `ISIN` | 前半句**规范**，三条具名约束合起来要求至少有一个，见契约 §4C.1；后半句 **CMOP**，见契约 §4.6 | 全部。标的是合成的，填 ISIN 等于声称一个不存在的注册标识 |
| V1-023-3 | `SctiesMvmntTp` 为 `DELI` 或 `RECE` | 规范，1..1 | 全部 |
| V1-023-4 | `Pmt` 取 `APMT` | **CMOP** | 全部。`FREE` 会让 `SttldAmt` 变成无意义字段 |
| V1-023-5 | `SctiesTxTp/Cd` 取 `TRAD` | **CMOP** | 全部。44 个取值里只有它是普通买卖交割 |
| V1-023-6 | `SttlmQty/Qty/Unit` 非空且为正 | 规范必填，正数是 **CMOP** | 全部 |
| V1-023-7 | `SfkpgAcct/Id` 非空 | **CMOP** | 全部。schema 上可选，不填就说不清这笔结算属于哪个账户 |
| V1-023-8 | `SttlmAmt` 存在，且 `CdtDbtInd` 与 `SctiesMvmntTp` 方向相容 | **`SttlmAmt` 存在是规范**，具名约束 `SettlementAmountRule`，见契约 §4C.1；`CdtDbtInd` 在 `AmountAndDirection94` 里是规范必填；**只有相容性是 CMOP** | `Pmt=APMT` 时 |
| V1-023-9 | `TradDt` 与 `SttlmDt` 均填，且 `SttlmDt` 严格晚于 `TradDt` | `SttlmDt` 规范必填；`TradDt` 是 SMPG 推荐而非规范必填，见契约 §4C；严格晚于是 **CMOP** | 全部 |
| V1-023-10 | 各参与方走 `PrtryId`，**不得出现任何 `AnyBIC` 值** | **规范**。`AnyBIC` 的取值必须是 ISO 9362 注册机构发布的真实 BIC，**合成机构没有注册过** | 全部。**XSD 只检查形状不检查注册，所以这条过得了 schema**，见[生成规范](data-generation-specification.md) §3B.5 |

**V1-023-8 是最值得单列的一条。** 借贷方向随收付方向翻转，
**这与 `AllocNetMoney` 的符号随 `Side` 翻转是同一类坑**，见契约 §3A.7。
生成器若两边同号，错误会以"金额对不上"的形式出现在很远的下游，
**那时已经无法判断是生成错了还是聚合错了**。

#### `sese.024` 状态回报

| 编号 | 检查 | 依据 | 适用条件 |
|---|---|---|---|
| V1-024-1 | `TxId/AcctOwnrTxId` 非空 | **CMOP**。顶层 `TxId` 是七元素标识组，组内各元素 schema 上并非都必填 | 全部 |
| V1-024-2 | **至少一个状态轴有值** | **规范**，四条互补具名约束，见契约 §4C.1 | 全部。四轴全空过得了 XSD，但违反规范约束 |
| V1-024-3 | `SttlmSts` 若存在，只能是 `Pdg` 或 `Flng`，不用 `Prtry` | **CMOP** | 有结算状态时 |
| V1-024-4 | `Pdg` 或 `Flng` 之下必须给 `Rsn` 或 `NoSpcfdRsn` | **规范**。两者都不给是 schema 非法 | 有结算状态时 |
| V1-024-5 | `Rsn` 恰好一条 | **CMOP**。`Rsn` 基数为 1..unbounded | S-1 与 S-2；多原因留给更后面的场景 |
| V1-024-6 | 原因码落在挂起与失败两表的 **57 个交集**内 | **CMOP** | 仅 S-2。只在一侧存在的码会让另一步无法生成 |

**V1-024-3 要解释一句。** `Prtry`（专有）在规范上完全合法，
**CMOP 不用它是因为专有码没有公开词表**，用了就等于自造标准，
与"由公开规范推导"这条可信度支点冲突，见契约 §3.1 的同类约定。

#### `sese.025` 结算确认

| 编号 | 检查 | 依据 | 适用条件 |
|---|---|---|---|
| V1-025-1 | `TxIdDtls/AcctOwnrTxId` 非空 | **CMOP** | 全部 |
| V1-025-2 | `FctvSttlmDt` 非空 | 规范，1..1 | 全部 |
| V1-025-3 | `SttlmDt` 也填 | **CMOP**。schema 上是 0..1 | 全部。**两个日期都填，延迟量才算得出来** |
| V1-025-4 | `SttldAmt` 非空 | **规范**。具名约束 `SettledAmountRule`，见契约 §4C.6.1。**2026-09-12 从 CMOP 改判**：上一批按 `SettlementAmountRule` 这个名字检索，而本报文上它叫 `SettledAmountRule` | `Pmt=APMT` 时。规范条件路径为 `/TransactionIdentificationDetails/Payment` 等于 `AgainstPaymentSettlement` |
| V1-025-5 | `FctvSttlmDt` 不早于 `SttlmDt` | **CMOP** | 全部。**提前结算不在任何已冻结场景内**，出现即为生成错误 |

#### 三条跨报文的具名约束，逐张报文都要评估

**它们不属于任何一张报文，属于本项目用到的每一张证券报文**，出处见契约 §4C.6。
**写在这里而不是各报文小节下，是为了避免抄三遍以后漏改一处。**

| 编号 | 检查 | 依据 | 适用条件 |
|---|---|---|---|
| V1-ALL-1 | 结算方链条 `Party1` 至 `Party5` 连续填写，不跳号 | **规范**。`Party2PresenceRule` 一族四条 | 全部证券报文。**`Party3` 填而 `Party2` 空即为违规，哪怕两者各自合法** |
| V1-ALL-4 | 对手方一侧的 `Dpstry` 与 `Pty1` 都非空 | **规范**。`DeliveringDepositoryAndParty1Rule` 与其镜像，见契约 §4C.7.1 | `sese.023` 与 `sese.025`，**且不带常设结算指令时**。CMOP 从不带，**故对全部数据恒适用**。方向决定看哪一侧：交收方向看收方块，收方向看交付方块 |
| V1-ALL-5 | 只带结算状态轴的 `sese.024`，**判为已匹配，不判为匹配状态缺失** | **规范**。`SettlementStatusAndMatchedRule`，见契约 §4C.7.3 | `sese.024`，且市场存在匹配过程。**已冻结场景全部满足该前提** |
| V1-ALL-2 | `AcctOwnrTxId` 为 `NONREF` 时，**不参与任何回连检查，也不参与去重与连接** | **规范**。`NoAccountOwnerTransactionIdentificationRule` | 全部。**CMOP 自身不产生 `NONREF`**，本条只在读取外部数据时起作用 |
| V1-ALL-3 | 链接结构若填 `LongNumber`，其值必须是被链报文的 XML 报文标识符 | **规范**。`ShortLongNumberRule` | 仅在填了链接结构时。**CMOP 一律不填 `ShortNumber`**，因为不产生 FIN 报文，这半句是 CMOP 决定 |

**V1-ALL-2 是三条里唯一一条永远沉默的。** 它存在的理由不是捕获 CMOP 的错误，
**是挡住一类实现**：把"引用字段必须能连回上游"写成无条件断言，
在外部数据上会把合规报文判成违规。**按 §6.2 的口径它必须有注入**，见 §6.4。

**V1-ALL-1 对生成器的要求比对校验层的要求大。** 校验层只是判断链条有没有洞，
**生成器必须先决定这一笔用几层结算方，再从 `Party1` 起连续填**——
槽位之间不是独立的，而[数据生成规范](data-generation-specification.md) §3B 写的时候
把它们当成独立的。

### 2A.4 V2：同一 `TxId` 的报文集合

**这一级的第一条不是检查，是解析前提，写在这里以免被当成可选项。**

**关联键在三种报文里有三个不同的路径**，见契约 §4.6.1：

| 报文 | 路径 |
|---|---|
| `sese.023` | `TxId` |
| `sese.024` | `TxId/AcctOwnrTxId` |
| `sese.025` | `TxIdDtls/AcctOwnrTxId` |

**三个名字、三层深度。因此关联键必须按报文类型分别指定路径，不能按字段名匹配。**
FIX 侧 `ClOrdID` 与 `AllocID` 在各报文里同名同形的习惯**在这里不成立**。

| 编号 | 检查 | 适用条件 |
|---|---|---|
| V2-1 | 每条 `sese.024` 与 `sese.025` 都能回连到一条存在的 `sese.023` | 全部 |
| V2-2 | 同一 `TxId` 下 `sese.024` 至少一条 | **所有 S 场景**。契约 S2-5 |
| V2-3 | 同一 `TxId` 下 `sese.025` 恰好一条 | **`PrtlSttlmInd=NPAR` 或缺席，且无冲正时**。契约 S2-6。适用条件已由场景改挂到 `PrtlSttlmInd` 字段上，见契约 §4C.3。**其余三个值下由 V2-15 与 V2-16 接管**，契约 §4D.13.6 |
| V2-4 | 同一 `TxId` 下报文的时点严格递增 | 全部 |
| V2-5 | `sese.025` 回带的标的、数量与账户与 `sese.023` 一致 | 全部 |
| V2-6 | `PrcgSts` 在同一 `TxId` 下不发生变化 | **仅 S-1 与 S-2**。契约 S2-4。**S-3 已于 2026-09-12 冻结并确实破坏了它**，见 §2A.5.1 |
| V2-7 | 撤销链的两个锚点都在：`sese.020` 回引原指令，`sese.027` 的 `CxlReqRef` 回引该 `sese.020` | 仅 S-3 与 S-4。**两个锚点方向相反，必须分别检查**，见契约 §4.6.3 |
| V2-8 | 撤销链报文的时点与原链**合并排序后**仍严格递增 | 仅 S-3 与 S-4。**不能两条链各自排序**，那样撤销请求可以排在结算确认之前而查不出来 |
| V2-9 | `sese.020/AcctOwnrTxId` 的取值不得是字面量 `NONREF` | 仅 S-3 与 S-4。契约 §4.6.6。**规范允许它，且允许多笔同时用它**，按此字段关联会全部互相命中 |
| V2-10 | `sese.020/CxlRsn/Cd` 必填 | 仅 S-3 与 S-4。**CMOP 决定**，规范是 0..1。**撤销的原因只能从请求侧回答**，成功侧词表只有两个值 |
| V2-11 | `sese.027/PrcgSts/Canc` 的 `Rsn/Cd` 取值必须落在 `CancelledStatusReason5Code` 内 | 仅 S-3。**该分支只有 `CANI` 与 `OTHR`**，填 `CANS` 或 `CSUB` 是词表错配 |
| V2-12 | `sese.027/PrcgSts/Dnd` 的 `Rsn/Cd` 取值必须落在 `DeniedReason6Code` 内 | 仅 S-4。**同族另有三个码集码字母重合**，见契约 §4.6.6 |
| V2-13 | `sese.020/TxDtls` 出现时，`FinInstrmId`、`SttlmDt`、`SttlmQty` 三者齐全 | 仅 S-3 与 S-4。**可选块反面**，与 C4-24 同类 |
| V2-14 | `PrtlSttlmInd=NPAR` 的指令下**不得**出现带 `PrtlSttlm` 的 `sese.025` | 全部。**否定式**，依据 `NPAR` 码值定义。契约 S9-1 |
| V2-15 | 同一 `TxId` 下带 `PrtlSttlm` 的确认按时点排序，前若干条为 `PAIN`，**恰好最后一条**为 `PARC` | 仅 `PrtlSttlmInd` ∈ {`PART`,`PARC`,`PARQ`}。**规范 guideline**（C30／C32）。契约 S9-2 |
| V2-16 | `PrtlSttlm=PARC` 出现时，同一 `TxId` 累计 `SttldQty` 等于 `SttlmQty` | 同上。**CMOP**，码值定义与 guideline 在"放弃剩余"时分歧。契约 S9-3 |
| V2-17 | `PrvsPrtlConfId` 出现时，命中同一 `TxId` 下时点更早的一条确认 | 仅当出现。**第三档锚点**。契约 S9-4 |
| V2-18 | `CANZ` 撤销后，回指它的新指令 `SttlmQty` 之和等于原指令 | 仅 S-9b。**CMOP**。契约 S9-5 |
| V2-19 | S-9b 的新指令各自的 `sese.025` **不得**带 `PrtlSttlm` | 仅 S-9b。**否定式**。契约 S9-6 |
| V2-20 | 判定 `PARC` 的每一处必须同时写明元素名或码集名 | 全部。**同一张 `sese.025` 里两个 `PARC` 意思无关**，契约 §4D.13.3。**这是 §2C.7 第五句在同报文内的第一个实例** |

**V2-11 与 V2-12 是词表成员资格检查在结算段的第一次出现。** 此前这一维度只在资金段有
（§2C.7）。**它们不能合并成一条"原因码合法"**：两个分支用的是不同的码集，
**而两个码集的码字母大量重合**，合并之后填错分支查不出来。

**V2-3 与 V2-6 的适用条件必须与检查同时发布。**
把它们写成无条件不变量，会在部分结算和撤销场景落地当天变成成批的假阳性，
**而假差异比漏报更致命**，见 §6.1 末尾。

### 2A.5 V3：场景契约

**这一级只在已知该链属于哪个场景时才可判定。**
未标注场景的链条**记为 not evaluated，不记为 pass**，见 §5。

| 编号 | 检查 | 适用条件 |
|---|---|---|
| V3-1 | 挂起报文的原因码与随后失败报文的原因码相同 | 仅 S-2。契约 S2-1 |
| V3-2 | 失败报文晚于挂起报文，且不早于 `SttlmDt` 结束 | 仅 S-2。契约 S2-2。**时点是结算日结束，不是次日开盘** |
| V3-3 | `FctvSttlmDt > SttlmDt` | 仅 S-2。契约 S2-3 |
| V3-4 | `FctvSttlmDt = SttlmDt` | 仅 S-1 |
| V3-5 | 结算数量等于该账户的 `AllocQty`，结算金额等于 `AllocNetMoney` | S-1 与 S-2。**这是结算段接回确认段的那一针** |

#### 2A.5.1 S-3 与 S-4 的场景契约：两条互为反面，必须同时写

**S-3 与 S-4 的区别本身就是可校验的契约**，见契约 §4.6.3 与 §4.6.4。

| 编号 | 检查 | 适用条件 |
|---|---|---|
| V3-6 | 撤销成功时，原 `TxId` 下**必须**有一条 `PrcgSts=Canc` 的 `sese.024` | 仅 S-3 |
| V3-7 | 撤销被拒办时，原 `TxId` 下**不得**出现任何新的 `sese.024` | 仅 S-4 |
| V3-8 | `sese.027` 的终态取 `Canc` 时原因码为 `CANI`，取 `Dnd` 时原因码为 `DSET` | S-3 与 S-4。**`CANS` 与 `CSUB` 会把撤销发起方说错** |
| V3-9 | S-3 有 `PdgCxl` 中间态，S-4 没有 | 分别适用。**这个不对称是有意的**，见契约 §4.6.4 |
| V3-10 | 撤销被拒办时，原指令的结算确认仍然存在且未被改写 | 仅 S-4。**撤销失败不改变任何既有事实** |

**V3-6 与 V3-7 必须同时存在。** 只写 V3-6，生成器在 S-4 上多发一条状态回报查不出来；
只写 V3-7，S-3 漏发第 6 步同样查不出来。**互为反面的两条断言，缺一条就等于两条都失效。**

**`sese.027` 不需要 V1-024-2 那样的补充校验。** 它的 `PrcgSts` 是 1..1，
**schema 本身就保证了至少一个状态**，见契约 §4.6.3。
**这是同族两张报文校验需求不同的第一例**，校验层不得按报文族一刀切。

#### 2A.5.2 S-5 的场景契约：一条在聚合处，其余都在报文里

**S-5 的检查分布很不均匀**，见契约 §4.6.5。

| 编号 | 检查 | 适用条件 |
|---|---|---|
| V3-11 | 每条 `sese.026` 的 `ConfRef` 恰好命中一条已存在的 `sese.025` | 仅 S-5。**规范必填 1..1，故命不中即为生成缺陷** |
| V3-12 | `sese.026` 的 `SctiesMvmntTp` 与被冲确认相同，**不翻转** | 仅 S-5。翻转即变成一笔新的相反交割 |
| V3-13 | `sese.026` 的 `SttldQty`、`SttldAmt`、`FctvSttlmDt` 与被冲确认逐字段相等 | 仅 S-5。**继承值不得重编** |
| V3-14 | `sese.026` **不计入**确认计数 | 仅 S-5。否则"确认恰好一条"会被误判 |
| V3-15 | 任何按已结算确认求和的聚合，**必须扣除被冲正的那笔** | **不限 S-5，适用于全部聚合口径** |

**V3-15 与其余四条不是一类东西。** 前四条判的是报文本身，**在 Bronze 就能判**；
V3-15 判的是聚合结果，**要到 Silver 或 Gold 才有对象可判**。
**把它写进本节是刻意的**：它是 S-5 唯一会造成错误数字的地方，
**而错误数字不会自己报警**——聚合永远算得出一个值，只是那个值多算了一笔。

**V3-11 的"恰好一条"比 S-3 的锚点检查强。** 撤销链的指令锚点是可选块，
**本条的确认引用是 1..1 必填**，所以命不中只能是生成缺陷，不存在合规的缺席情形。

**V3-2 是整个结算段最值得测的一条判定**，理由见契约 §4.5：
挂起与失败共用同一批原因码，**区分它们的是结算日而不是原因本身**。
主 BO 的 T+1 早晨异常处理流要证明的正是这件事。

### 2A.6 本层不做什么

**列出来是为了防止将来把缺口误当成通过。**

- **两个撮合轴一律不检查。** S-1 与 S-2 都不涉及撮合分支。
  撮合状态**不是单调的**，已撮合的指令可以退回未撮合，
  **按状态机去校验它是错的**，见契约 §4.4.1。
- ~~**部分结算不检查。**~~ **2026-09-13 解锁**，S-9 冻结，检查见 V2-14 至 V2-20，契约 §4D.13。
  **`PARC`／`PARQ` 的每次下限仍不检查**：三张报文里没有找到承载下限的元素。
- **`Prtry` 分支不检查**，因为 CMOP 不生成它，见 V1-024-3。
- **本层不判断外部一致性。** R3 与 R4 是对账，不是校验。

### 2A.7 与故障注入的对应

**每一条 V3 检查都要有一条注入证明它有效**，这是 §6 的规则。
**完整清单已于 2026-09-12 补齐，见 §6.4**，共十一条，覆盖 V1、V2、V3 三级。
此前登记的"待 S-3 与 S-4 解锁后统一补"已不成立：**多数注入不依赖未解锁的场景**，
只有三条带失效登记。

## 2B. 分配与确认段的断言

**编号在 §2A 之后，但链条上这一段在结算段之前。** 编号只反映写作次序。

本节回应[监管与行业数据契约](regulatory-data-contracts.md) §3A.9 的两条要求：
补一条确认层断言，以及把 §2.3 的 R1 扩展到分配侧。

### 2B.1 这一段的失效方式与结算段不同

**结算段的问题是 schema 挡不住空报文**，见 §2A.1。
**分配与确认段的问题不在这里**：FIX 4.4 的必填列挡得住一部分，
而且这一段的等式大多有规范原文背书。

**这一段的问题是断言本身容易写得过宽。**
[契约文档](regulatory-data-contracts.md) §3A.7 记录了同一个错误连续出现三次，
每次都是**把一个在窄场景下成立的等式写成普遍不变量**：

1. `CumQty + LeavesQty = OrderQty`，撤单订单上不成立。
2. 总分配数量等于被分配订单的成交总量，**规范自己的脚注**举了 GT 订单与跨日均价两个反例。
3. 每个分配账户恰好一条确认，Cancel/Replace 与抄送确认都会破坏它。

**三次的错法相同，因此本节的规则也只有一条：每条断言必须同时给出适用条件与失效条件。**
失效条件写的是"哪个场景扩展后这条不再成立"，
**与 §6.1 要求每条场景契约类注入登记失效场景是同一条纪律**。

### 2B.2 断言清单

**"性质"一列区分三种来源，可改动性依次递增：**
FIX 明确的不能改；CMOP 决定的可以改但要改口径文档；
**CMOP 推论的最危险**，因为它由两条断言合成，**上游任何一条收窄，它就跟着失效**。

| 编号 | 断言 | 性质 | 适用条件 | 失效于 |
|---|---|---|---|---|
| VA-1 | 总分配数量等于 `Quantity`(53) | FIX 明确 | 无条件 | — |
| VA-2 | 组内各 `OrderBookingQty`(800) 之和等于 `Quantity`(53) | FIX 明确 | 该字段出现时 | — |
| VA-3 | `GrossTradeAmt`(381) 等于 Σ(`AllocQty` × `AllocAvgPx`) | FIX 明确 | 该字段出现时 | — |
| VA-4 | `NetMoney`(118) 等于 Σ`AllocNetMoney`(154) | FIX 明确 | 该字段出现时 | — |
| VA-5 | 卖出时 `AllocNetMoney` 等于成交额减佣金减各项费用，买入时为加 | FIX 明确 | 该字段出现时 | — |
| VA-6 | **不得断言** `Quantity`(53) 等于被分配订单的成交总量 | **规范明确否定** | 仅无 GT 订单、无跨日均价时成立，A-1 满足 | 任何引入 GT 或跨日均价的场景 |
| VA-7 | `AllocTransType` 为 Replace 或 Cancel 的分配指令必带 `RefAllocID`(72) 与 `AllocCancReplaceReason`(796) | FIX 明确 | 无条件 | — |
| VA-8 | `AllocStatus=2` 的应答上，`AllocRejCode`(88) 与 `NoAllocs`(78) 组**不得两者皆空** | FIX 明确 | 无条件 | — |
| VA-9 | `AllocStatus` 不为 2 的应答上**不得出现** `NoAllocs`(78) 组 | FIX 明确 | 无条件，**方向为禁止** | — |
| VA-10 | Replace 型分配指令携带的字段集合与被替换的那条相同 | FIX 明确 | 无条件 | — |
| VA-11 | 沿 `RefAllocID` 回溯终止于一条 `AllocTransType=New` 的分配指令，且链上无环 | **CMOP 决定** | 无条件 | — |
| VA-12 | **不得断言** Replace **或 Cancel** 之后必然到达 `AllocStatus=0` | **规范明确否定** | 规范允许对方在已发 Accepted 后拒绝 Replace 或 Cancel（2026-09-13 补 Cancel） | — |
| VA-13 | `AllocStatus` 的合法域是 0 至 5 六个值，**不是报文页上的四个** | FIX 明确 | 无条件 | — |
| VA-14 | `IndividualAllocRejCode`(776) 的取值域与 `AllocRejCode`(88) 相同，**且只能由校验层保证** | FIX 明确 | 无条件；FIXML 对 776 不设枚举 | — |
| VA-15 | R1 分配侧只计有效分配：被已接受的 Cancel 或 Replace 回引的原件不计，Cancel 件本身不计 | **CMOP 决定** | 无条件；契约 §3A.11a | — |
| VA-16 | **不得断言**重下的 New 能沿字段回溯到被撤销的原件 | **规范结构上不支持** | 无条件 | — |
| VA-17 | `AllocTransType=New` 的分配指令不带 `RefAllocID`(72) | 字段定义，**第二档** | 无条件 | — |
| VA-18 | 分配更正对照表逐行：同订单、`Quantity` 相等、重下晚于撤销被接受 | **CMOP 决定** | 仅生成侧，输入为对照表 | 接入真实数据时，**对照表不存在** |
| VC-1 | 每个分配账户**至少**一条确认 | FIX 明确 | 无条件 | — |
| VC-2 | 每个分配账户**恰好**一条确认 | **已失效** | 曾要求全为 New、无抄送、无 Cancel/Replace | **已于 C-2 冻结时失效。C-2 让被拒账户出现两条确认，且两条都不是抄送** |
| VC-3 | 一条确认内 Σ`OrderCapacityQty`(863) 等于该报文的 `AllocQty`(80) | FIX 明确 | 无条件 | — |
| VC-4 | 各账户确认的 `AllocQty`(80) 之和等于分配指令的 `Quantity`(53) | **CMOP 推论** | 由 VA-1 与 VC-2 合成，**故只在 VC-2 的窄条件下成立** | 与 VC-2 同时 |
| VC-5 | 抄送确认排在主确认被应答之后 | FIX 明确 | `CopyMsgIndicator = Y` 且买方走 Model 1 | — |
| VC-6 | `AvgPx` 是**毛价**，不是扣费后的净价 | FIX 明确 | 无条件 | — |
| VC-7 | `ConfirmTransType` 为 Replace 或 Cancel 的确认必带 `ConfirmRefID`(772) | FIX 明确 | 无条件 | — |
| VC-8 | `AffirmStatus = 2` 的应答必带 `ConfirmRejReason`(774) | **CMOP 口径** | 规范原文的条件句写错了字段，见契约文档 §3A.11 | — |
| VC-9 | Replace 型确认携带的字段集合与被替换的那条相同 | FIX 明确 | 无条件 | — |
| VC-10 | 沿 `ConfirmRefID` 回溯终止于一条 `ConfirmTransType=New` 的确认，且链上无环 | **CMOP 决定** | 无条件；与 VA-11 同形 | — |
| VC-11 | 只有 Model 1 能到达 `AffirmStatus = 3`；抄送与状态播报的终态是 `1` | FIX 明确 | 无条件，**且与规范的一句总述冲突** | — |
| VC-12 | 按 `AffirmStatus` 统计拒绝必须先按 `CopyMsgIndicator`(797) 分层 | **CMOP 口径** | 无条件 | — |
| VC-13 | **不得**为确认侧的拒绝原因建立分布口径 | **词表明确不支持** | 无条件 | — |
| VC-14 | 每个分配账户至少一条**有效**确认：未被 Cancel 或 Replace 回引，且本身不是 Cancel | **CMOP 决定** | 无条件；**收紧 VC-1** | — |
| VC-15 | **不得断言**重下的 New 能沿字段回溯到被撤销的确认 | **规范结构上不支持** | 无条件 | — |
| VC-16 | **不得断言** Cancel 型确认必有应答，**也不得断言必无** | **规范流程图不画，且该页有粘贴痕迹** | 无条件，**双向** | 规范后续版本明确时收窄 |
| VC-17 | `ConfirmTransType=New` 的确认不带 `ConfirmRefID`(772) | 字段定义，**第二档** | 无条件 | — |
| VC-18 | 确认更正对照表逐行：同分配账户、同 `AllocID`、重下紧随撤销 | **CMOP 决定** | 仅生成侧，输入为对照表 | 接入真实数据时，**对照表不存在** |

**VA-6 是一条否定式断言，写进清单是为了防止它被重新发明。**
它看上去比 VA-1 更自然，**而且在 A-1 上恰好成立**，
所以不写下来，日后很容易有人把它当成 VA-1 的等价说法补回去。

**VC-4 是唯一的推论型断言，标注要一直带着。**
它不是规范说的，是 VA-1 与 VC-2 合成出来的。
~~**VC-2 一旦在 C-2 失效，VC-4 同时失效**~~
**C-2 已于 2026-09-12 冻结，VC-2 与 VC-4 双双失效，这不再是预告而是现状。**
**当初写下这条连带关系是有回报的**：VC-4 的形式里看不出它依赖 VC-2，
若没有那行标注，C-2 落地后 VC-4 会继续跑绿并继续给出错误结论。

**VC-13 是清单里第一条"不得建立某种口径"的条目。**
它不针对数据，针对的是分析口径本身。**确认侧的拒绝原因词表只有三个取值**，
真实原因（净额、毛额、费用算不平）一个都不在其中，全部落到 99 = Other。
**再多样本也分不出细类，所以这是词表的性质，不是采样不足**，
写下来是为了挡住"加量就能分析"这个自然但错误的下一步。

**VC-12 依赖的分层键本身不受 schema 保护。**
`CopyMsgIndicator`(797) 是 Boolean，**但 FIXML 把它声明为 `(#PCDATA)` 而不带取值枚举**，
同为 Boolean 的 `LegalConfirm`(650) 却带。**FIXML 的严格程度是逐字段的，不由类型决定**，
这一点与 VA-14 记的是同一个现象，**两条合起来意味着校验层不能按类型推断覆盖面**。

**VA-9 与 VA-12 的方向要看清。** 两条都不是"某条件下必填"，
**VA-9 是"其他条件下禁止出现"，VA-12 是"不得写出某条断言"**。
清单里现在有三条这种反向条目（VA-6、VA-9、VA-12），
**它们共同的失效方式是被人当成冗余删掉**，因为正向检查跑绿时它们看不出贡献。

**VA-11 是清单里第一条不是等式的断言。**
VA-1 到 VA-10 与 VC-1 到 VC-6 全都是"按键聚合后比数值"，
**VA-11 要的是沿引用回溯直到根节点**，形状不同，
成本也不在断言本身而在校验层此前没有这种检查。
~~**它一旦落地，A-2b 的两跳链就是它的第二个用例**，
而两跳与一跳共用同一条断言这件事，正是把它写成回溯而不是写成"必有前驱"的理由。~~
**2026-09-13 更正：A-2b 不是两跳链**（契约 §3A.10 第 2 条）。重新下达的 New 不带 `RefAllocID`，
VA-11 在它上面零跳即终止。**写成回溯而不写成"必有前驱"的理由仍然成立，但换了一个**：
Replace 可以接在 Replace 之后，链长没有规范上限，而"必有前驱"会把合规的 New 判成孤儿。

**VA-11 的回溯结果不能直接当 R1 的输入（2026-09-13，随 A-2b 冻结）。**
Replace 链的末端就是有效版本，**Cancel 链的末端却是撤销件本身**。
所以 R1 取有效分配靠 VA-15 的"排除被回引者"，不靠"取链末端"；
**两种写法在只有 Replace 的 A-2 上结果相同，在 A-2b 上结果相反**，
这正是 A-2b 要冻结的理由。VC-14 与 VC-1 的关系同理。

**VA-11 与 VC-10 已于 2026-09-13 实现**，见
[`validation/`](../../../validation/README.md) 的 `chain.py`。实现形状是**指针倍增**：
每轮把"父指针"替换为"父的父"，轮数是链长的对数，**不依赖递归 CTE**，
因此能照搬成 Spark 3.5 SQL 的有限轮自连接。环在固定轮数后仍未到达根即判出，**不会死循环**。
**SQL 版本同日落地**（`validation/sql/chain_traversal/`），在 DuckDB 上与参考实现逐行比对一致；**每轮必须物化**，嵌套 CTE 会让计划规模随轮数指数增长。

**VA-14 记的是一处 schema 管不到的地方。**
块层原因码在 FIXML 里是带枚举的，账户层原因码是 `(#PCDATA)`。
**同一个取值域，一边 schema 挡得住，一边挡不住**，
所以账户层的域检查必须由 CMOP 自己写，且不能因为"schema 已经校验过"而省掉。

**VC-6 不是等式而是口径。** 生成器若把扣费后的价格填进 `AvgPx`，
**金额三件套会自洽地全错，每一条单独看都对**。
这类错误等式检查不出来，只能靠口径本身写死。

### 2B.3 R1 与 R2 在分配层的具体化

§2.3 目前只在 L-1 范围内具体化，A-1 与 C-1 冻结后补上这一层。

| | R1 在分配层 | R2 在分配层 |
|---|---|---|
| 左侧 | L-1 的成交事实，按 `ExecID` 去重 | 同左 |
| 右侧 | 各账户 `AllocQty` | 各账户 `AllocNetMoney` |
| 聚合式 | Σ`AllocQty` 对 `Quantity`(53) | Σ`AllocNetMoney` 对 `NetMoney`(118) |
| 左侧日期基准 | `TransactTime`(60) 所属交易日 | 同左 |
| 右侧日期基准 | 分配指令的 `TransactTime` 所属交易日 | 同左 |
| 容差 | 零 | 仅允许舍入，数值待定 |

**这一层两侧的日期基准相同，与 §2.3 的 R2 不同。**
分配发生在交易日，不跨结算日，**所以不存在 T 到 T+1 的错位**。
**这一条要显式写出来**，否则会有人照抄 §2.3 的 T+1 映射，
**然后把全部分配报成时点差异**。

**买卖方向进入金额公式。** `AllocNetMoney` 的符号随 `Side` 翻转。
生成器两边同号时，差异会以"金额对不上"的形式出现在 R2，
**而根因在生成器不在管道**。这是同一个符号坑的第一次出现，
后面还有两次，见 §2A.3 的 V1-023-8 与契约 §4B.4 的 P1-5。

### 2B.4 时间轴与去重：这一层的断言不针对某张报文

**依据是[契约文档](regulatory-data-contracts.md) §3A.12**，那一节从 FIX 会话层取得三条时间轴
与两套重复机制。**本节的断言与 §2B.2 的不同：它们不针对某张报文的字段等式，
而针对整条管道怎样解释时间与重复。**

| 编号 | 断言 | 性质 |
|---|---|---|
| VT-1 | 迟到量的是**摄入批次归属之差**，不是任何两个时间戳之差 | **CMOP 决定**，因为会话层不允许迟到超过两分钟 |
| VT-2 | `SendingTime`(52) 与原子钟之差超过两分钟的报文**不可能存在于合法样本中** | FIX 明确 |
| VT-3 | `OrigSendingTime`(122) 等于 `SendingTime` 时记为**未知**，不记为零延迟 | **CMOP 口径** |
| VT-4 | `OrigSendingTime` 不得大于 `SendingTime` | FIX 明确，**但这是被协议强制的，不得因此省掉方向检查** |
| VT-5 | `PossDupFlag`(43)=Y 的重复按**会话加序号**去重 | FIX 明确 |
| VT-6 | `PossResend`(97)=Y 的重复**按业务键去重**，序号不同，协议层判不了 | FIX 明确把该责任交给应用 |
| VT-7 | Bronze 必须保留会话层字段 | **CMOP 决定**，失效场景见下 |
| VT-8 | 分配侧的迟到只能按营业日量，**因为分配指令上唯一必填的时间信息是 `TradeDate`(75)** | FIX 明确 |
| VT-9 | 确认侧可按秒量停留时长，**但跨结算日的判定仍按营业日** | **CMOP 口径** |
| VT-10 | 迟到导致的重述必须保留重述前后两个版本 | **CMOP 决定**，主 BO 的验收判据 |
| VT-11 | 批次 T 报出"分配缺失"是**正确行为**，不得为了让批次干净而抑制它 | **CMOP 决定** |
| VT-12 | 停留时长与跨界标志**分两个字段存**，不得合成一个 | **CMOP 口径** |

**VT-3 是一条容易被"简化"掉的口径。** 把未知记成零，
**每一条样本单独看都合法**，只有重传延迟的整体分布会向左偏。
**这类失效等式检查不出来**，与 VC-6 的净价问题同类。

**VT-7 有一个具体的失效场景，这是它从原则变成要求的原因。**
`PossResend` 的报文体与原件**完全相同，`TransactTime` 也相同**。
**丢掉会话头，重发件与原件在 Silver 里就不可区分**，
而 VT-6 要求的业务键去重此时无从判断哪一条是重发。
**原则性的"原样保留"挡不住实现时把会话头当噪声删掉，这个场景挡得住。**

**VT-1 与 VT-2 一起读才完整。** 单看 VT-1 像是 CMOP 图省事选了批次口径；
**加上 VT-2 才知道别的口径根本造不出合法数据**。
**VT-8 是第三条独立佐证**：分配指令上唯一必填的时间信息是一个日期，
**连按秒量的原材料都没有**。三条来自三个互不相关的地方，结论相同。

**VT-10 与 VT-11 是这一层里唯二不判数据对错的断言。**
VT-11 说的是某条告警**应当**出现，VT-10 说的是某个历史版本**应当**留着。
**两条都不会因为数据变脏而失败，只会因为实现图省事而失败**——
把批次 T 的结果原地覆盖，两条同时静默失效，**而所有等式检查照样跑绿**。
**A-3 的验收判据全部落在这两条上**，见契约文档 §3A.13。

**VT-12 挡的是一次很自然的合并。** 停留 23 小时未跨营业日，与停留 2 小时但跨了营业日，
**合成一个字段之后无法区分，而这两种在业务上是完全不同的事**。

### 2B.5 本节不做什么

- **不覆盖卖方主动发起的分配回报流程。** 报文已核对，但 CMOP 的业务设定里没有这条流，
  优先级明确定为低，见契约 §3A.8。
- **不覆盖报文分片。** `TotNoAllocs` 与各分片 `NoAllocs` 的求和式规范明确，
  但 CMOP 不生成分片报文。**登记在此，供将来使用。**
- **不重复 §2A 的三级划分。** 这一段的检查按报文类型天然分层，
  没有结算段那种"同一关联键三个路径"的解析难点。

## 2C. 资金段的业务校验层

**本节回应[监管与行业数据契约](regulatory-data-contracts.md) §4A.4 末尾提出的要求**，
覆盖 `pacs` 与 `camt` 两族。与 §2A 同样只规定校验什么，不规定实现。

### 2C.1 先说为什么不能照搬结算段

§7 把这一层登记为未决项时写了一句"三级划分应当可以照搬，但照搬前要先核实"。
**核实结果是不能照搬**，四条结构差异各自动了一处骨架。

**一、`pacs` 是批量结构，`sese` 不是。** 一条 `pacs.009` 里 `CdtTrfTxInf` 是 1..unbounded，
一条报文藏着 N 笔业务事实。**§2A 的 V1"单条报文内部"在这里会同时指两种东西**：
组头内部，和某一笔交易行内部。**两者的判定时机与失败后果都不同**——组头错一次，
整批 N 笔全部作废；交易行错一次，只作废那一笔。**因此这一级必须拆成两级**，
这是本节与 §2A 最大的形状差异。

**二、必填叶子比结算段还少，`pacs.002` 交易层是零。** `PaymentTransaction177` 十九个元素
**一个都不必填**。§2A 的"依据"一列里还能写出若干"规范"，**本节的 `pacs.002` 部分几乎
全是 CMOP**。这不是本节写得随意，是规范确实没要求。**归属要如实标，不能为了好看而硬套。**

**三、词表不在 schema 里，因此校验带版本。** `External*` 类型在 XSD 里只是
`minLength` 1、`maxLength` 4 的字符串（契约 §4A.5）。**"这个码是否合法"在结算段是 XSD 的事，
在资金段是本层的事**，而且答案随词表季度版本变化。**§2A 里没有任何一条检查带版本，
本节有一整类。**

**四、`camt` 的一条明细可以覆盖多笔支付。** MDR 样例里有一条明细是 `Btch`、`NbOfTxs=20`
（契约 §4C.5）。**"一条支付对一条明细"这种按条数对账的写法在这里是错的**，
而结算段的 V2-3"恰好一条"正是那种写法。**照搬会在 P-4 落地当天变成成批假阳性。**

### 2C.2 四级，按判定所需的信息范围划分，外加一条正交维度

| 级 | 判定范围 | 执行时机 | 在 §2A 里的对应 |
|---|---|---|---|
| C1 | 单个组头内部 | Bronze 组层落地后 | **无对应，新增** |
| C2 | 单笔交易行内部 | Bronze 交易层落地后 | 对应 V1 |
| C3 | 同一 `EndToEndId` 的报文集合 | Silver 拼链条时 | 对应 V2 |
| C4 | 资金段与证券段之间 | 两段都拼完 | 对应 V3，但**判定的是跨段闭合而不是场景契约** |

**另有一条正交维度：词表成员资格。** 它不是第五级，因为它**在 C1 至 C4 每一级都出现**，
判定范围与所在级相同，只是多带一个版本参数。**单列一小节，见 §2C.7。**

**C1 与 C2 的分级依据是失败爆炸半径，不只是信息范围。** 组头违规作废整批，
交易行违规只作废一笔，**两者不能进同一个队列**，否则一条坏组头会被记成 N 条差异。

### 2C.3 C1：组头内部

| 编号 | 检查 | 依据 | 适用条件 |
|---|---|---|---|
| C1-1 | `MsgId`、`CreDtTm`、`NbOfTxs`、`SttlmInf` 四项非空 | **规范**，`GroupHeader131` 必填四项 | 全部 `pacs.008`/`pacs.009` |
| C1-2 | `NbOfTxs` 等于该组实际 `CdtTrfTxInf` 条数 | **CMOP**。schema 只保证字段存在，不保证一致。契约 P1-1 | **所有 `pacs` 报文** |
| C1-3 | `CtrlSum` 若存在，等于各 `IntrBkSttlmAmt` 之和 | **CMOP**。契约 P1-2 | **仅在 `CtrlSum` 出现时可评估**；P-1 一律填 |
| C1-4 | `SttlmInf/SttlmMtd` 取 `CLRG` | **CMOP**。四个取值 schema 内联，都合法 | 全部。理由见契约 §4B.3 |
| C1-5 | `MsgId` 在同一摄取批次内唯一 | **CMOP** | 全部。重复 `MsgId` 会让 C3 的回连产生歧义 |
| C1-6 | 组头 `IntrBkSttlmDt` 非空 | **规范**（异或的另一半），见契约 §4C.8.1 | 全部 `pacs.009`。**它与 C2-4 是同一条规则的两面，必须成对实现** |
| C1-7 | `SttlmAcct` 与三个 `RmbrsmntAgt` 一律缺席 | **规范**。`SettlementMethodClearingRule`：`SttlmMtd=CLRG` 禁止这四个元素，见契约 §4C.8.3 | 全部。**四个元素 schema 上都可选，XSD 判不出来** |
| C1-8 | `TtlIntrBkSttlmAmt` 缺席 | **CMOP** | 全部。**填了就把结算日锁在组头层**（`TotalInterbankSettlementAmountAndDateRule`），而 CMOP 不想为此放弃将来下移的余地 |

**C1-2 是本节唯一一条对全部 `pacs` 报文无条件成立的检查。** 其余四条都带条件，
**包括看起来最像规范的 C1-3**——`CtrlSum` 是选填，**写成无条件不变量就会在任何一条不填
`CtrlSum` 的合法报文上误报**。这与 §2A.4 记下的是同一类错误，已是第几次遇到不再计数。

### 2C.4 C2：单笔交易行内部

#### `pacs.009` 交易行

| 编号 | 检查 | 依据 | 适用条件 |
|---|---|---|---|
| C2-1 | `PmtId`、`IntrBkSttlmAmt`、`Dbtr`、`Cdtr` 四项非空 | **规范**，`CreditTransferTransaction79` 必填四项 | 全部 |
| C2-2 | `PmtId/EndToEndId` 非空 | **规范**，`PaymentIdentification13` 里唯一的 1..1 | 全部 |
| C2-3 | `IntrBkSttlmAmt` 为正，且币种与该块一致 | **前半句 CMOP，后半句规范**（`TotalInterbankSettlementAmountRule`，见契约 §4C.8.2）。**规范那半句只在组头填了总额时成立，CMOP 不填**，所以现在两半都靠 CMOP 撑着 | 全部 |
| C2-4 | **交易层 `IntrBkSttlmDt` 必须缺席** | **规范**。`GroupHeaderInterbankSettlementDateRule` 与 `TransactionInterbankSettlementDateRule` 构成异或，见契约 §4C.8.1。**2026-09-12 整条改写：原写"非空"，方向是反的** | **仅在组头填了日期时**，即 CMOP 现有全部取值 |
| C2-5 | `PmtTpInf/CtgyPurp/Cd` 取 `DVPM` 或 `RVPM`，与该链 `Side` 相容 | **CMOP** | 仅 DVP 场景 |
| C2-6 | 各 `UndrlygAllcn/Amt` 之和等于本行 `IntrBkSttlmAmt` | **CMOP**。契约 P1-3，**schema 完全不要求** | **仅在 `UndrlygAllcn` 出现时** |
| C2-7 | 每条 `UndrlygAllcn` 带 `RltdRefs/SctiesSttlmTxId` | **CMOP**。规范为回连备了这个字段，但它可选 | 仅 DVP 场景 |
| C2-8 | `UndrlygAllcn/CdtDbtInd` 与证券段同笔的 `SttlmAmt/CdtDbtInd` 相反 | **CMOP**。契约 P1-5 | 仅 DVP，且证券段已落地——**否则推到 C4** |
| C2-8a | `Dbtr` 与 `Cdtr` 走专有标识，**不得填 `BICFI`** | **规范**，理由同 V1-023-10 | 全部。合成机构未注册 BIC |

#### `pacs.002` 交易行

**这张表的"依据"一列没有一个"规范"，因为交易层零必填。**

| 编号 | 检查 | 依据 | 适用条件 |
|---|---|---|---|
| C2-9 | `OrgnlEndToEndId` 非空 | **规范**。`OriginalGroupInformationAbsenceRule`：组头层回连信息缺席则交易层必填，见契约 §4C.8.5。**2026-09-12 从 CMOP 改判** | 全部，**因为 CMOP 不填组头层回连信息**。改成组头层回连则本条失效 |
| C2-10 | `TxSts` 非空 | **CMOP** | 全部。不带状态的状态回报合法但无信息量 |
| C2-11 | `TxSts` 落在 `ExternalPaymentTransactionStatus1Code` 的 `Registered` 码内 | **CMOP**，见 §2C.7 | 全部 |
| C2-12 | `TxSts` 取 CMOP 使用的七个值之一 | **CMOP** | 全部。二十五个值里其余十八个属支票、现金提取等分支 |
| C2-13 | `TxSts` 为 `RJCT` 时 `StsRsnInf` 存在 | **CMOP**。schema 不要求 | 拒绝分支；**P-1 不产生，登记备用** |
| C2-13a | `Rsn/Cd` 不得取 `NARR` | **CMOP**。取 `NARR` 则 `StatusReasonRule` 要求必须写自由文本，与不生成自由文本的口径冲突，见契约 §4C.8.4。**`camt` 侧的 `ReturnReasonRule` 一字不差，故本条对资金段全部报文成立** | 全部 |
| C2-13b | 组状态缺席时，`StsRsnInf/AddtlInf` 一律缺席 | **规范**。`StatusReasonInformationRule` 的适用条件是组状态存在；**CMOP 不填组状态，故本条实为 CMOP 的自我约定** | 全部。**依据一列此处特意写两句，因为它看着像规范而实际不是** |
| C2-14 | `FctvIntrBkSttlmDt` 存在 | **CMOP** | **仅 `TxSts=ACCC` 时**。其余状态下它没有意义 |

**C2-10 与结算段的 V1-024-2 看着像，性质完全不同。** V1-024-2 现在是规范要求，
有四条互补的具名约束撑着（契约 §4C.1）；**C2-10 背后什么都没有**，
`pacs.002` 的 MDR 里没有同类具名约束，**它纯粹是 CMOP 不愿意生成空状态回报**。
**两条检查形状相同、可改动性完全相反**，这正是"依据"一列存在的理由。

#### `camt.053` 明细项

| 编号 | 检查 | 依据 | 适用条件 |
|---|---|---|---|
| C2-15 | `Amt`、`CdtDbtInd`、`Sts`、`BkTxCd` 四项非空 | **规范**，`ReportEntry16` 必填四项 | 全部 |
| C2-16 | `Sts` 落在 `ExternalEntryStatus1Code` 的四个值内 | **CMOP**，见 §2C.7 | 全部 |
| C2-17 | 族不具体时，子族**不得**具体 | **规范**。`FamilyAndSubFamilyRule`，见契约 §4C.9.2 | 全部 |
| C2-17a | `BkTxCd` 的域、族、子族构成**存在于组合表中的组合** | **CMOP**。见契约 §4A.9：三者是组合表，**不是三个独立枚举**。**规范只管层级关系，不管组合存在性** | 全部。组合表 1567 行，CMOP 只用 `PMNT` 与 `SECU` 两域共 829 行 |
| C2-17c | 借方明细取 `PMNT/ICDT/FICT`，贷方明细取 `PMNT/RCDT/FICT` | **CMOP** | 全部资金段明细。**两边都填 `ICDT` 也是合法组合**，所以组合表查不出方向错，**必须单列一条** |
| C2-17b | `BkTxCd` 内部至少有域或专有码之一 | **规范**。`DomainOrProprietaryRule` | 全部。**内部全空的 `BkTxCd` 在 schema 上合法** |
| C2-18 | `BkTxCd` 的域取 `PMNT` 或 `SECU` | **CMOP** | 全部 |
| C2-19 | `BookgDt` 与 `ValDt` 均存在 | **CMOP**。schema 上可选 | **仅 `Sts=BOOK` 时**。挂起明细还没有入账日 |
| C2-20 | 至少一条 `Bal`，且含 `OPBD` 与 `CLBD` 各一 | `Bal` 1..unbounded 是**规范**；两个类型各一是 **CMOP** | 仅 `camt.053`。`camt.052` 可以不带余额，`camt.054` 没有这个元素 |
| C2-21 | 消息分页与对账单分页**不得同时出现** | **规范**。`MessageOrStatementPaginationRule`，见契约 §4C.9.4 | 全部 `camt.053`。**两者都缺席是允许的**，CMOP 两者都不填 |
| C2-22 | `PmtTpInf` 与 `LclInstrm` **不得同时出现** | **规范**。`PaymentTypeOrLocalInstrumentRule` | 全部明细。**同上，都缺席允许** |

**C2-17 是本节唯一一条需要查外部表才能判定的检查**，其余的词表检查都是集合成员判定。
**组合表约 1569 行，三层各自采样会生成大量形式合法但组合不存在的交易码**，
而这类错误在任何 schema 校验里都不会暴露。

#### P-3 退汇的检查，按既有四级归位

**出处见契约 §4B.10。** 本场景不新增层级。

| 编号 | 检查 | 依据 | 适用条件 |
|---|---|---|---|
| C1-9 | `GrpRtr=false` 时 `NbOfTxs` 等于 `TxInf` 条数，且至少一条 `TxInf` | **规范**。`GroupReturnAndNumberOfTransactionsRule` 与 `GroupReturnAndTransactionInformationPresentRule` | 全部 `pacs.004`。**同一类错误的第四次出现** |
| C1-10 | `GrpRtr=true` 时 `CtrlSum` 与 `TxInf` 均缺席 | **规范**，两条约束 | 全部 `pacs.004`。~~CMOP 不生成整组退汇~~ **P-6 已于 2026-09-12 冻结，本条现在有生成侧用例** |
| C1-11 | ~~`GrpRtr=true` 时组层理由必填~~ **这条是错的** | **规范明确否定** | `GroupReturnAndReturnReasonRule` 的形式化多一个条件：**只在已填 `AddtlInf` 时理由才必填**。契约 §4B.11 |
| C1-12 | `GrpRtr=true` 时 `TtlRtrdIntrBkSttlmAmt` 缺席 | **CMOP 口径** | 全部。绕开 `C72` 缺守卫造成的歧义，契约 §4B.11 |
| C1-13 | `GrpRtr=true` 时 `NbOfTxs` 等于原批笔数 | **Guideline 级**，CMOP 选择满足 | 全部。**Rule 级在此处什么都不要求** |
| C1-14 | `GrpRtr` 缺席的 `pacs.004` 判为契约违规 | **CMOP 决定** | 全部。**缺席时五条约束一并失效**，见下 |
| C2-23 | `OrgnlEndToEndId` 非空 | **CMOP**。schema 上 0..1——**交易块里只有退汇金额是必填** | 全部。**第三次遇到"回连锚点在规范上可选"** |
| C2-24 | `Rsn/Cd` 取 `UPAY`，`AddtlInf` 缺席 | **CMOP** | 全部 |
| C2-25 | `UndrlygFICdtTrf` 缺席 | **规范**。`UnderlyingFinancialInstitutionCreditTransferRule`：原交易没有它就不许出现，**而 CMOP 的原划拨带的是 `UndrlygAllcn`** | 全部 |
| C4-13 | `RtrdIntrBkSttlmAmt` 等于原 `IntrBkSttlmAmt`，全额 | **CMOP**。契约 P3-1 | 全部 |
| C4-14 | 每条退汇的券腿存在一条 `sese.026` 冲正 | **CMOP**。契约 P3-3 | **仅 DVP，且仅逐笔退汇**。**本场景唯一一条必须同时看两段的检查** |
| C4-15 | 整组退汇时，原批次内每一笔都有券腿冲正 | **CMOP**。契约 P6-5，**C4-14 的批次级改写** | **仅 DVP，仅整组退汇**。展开原批次靠 `OrgnlMsgId` 一个字符串 |
| C4-16 | **不得断言**整组退汇能定位到笔 | **规范结构上不支持**。契约 P6-7 | 全部。`OrgnlEndToEndId` 只活在被禁止出现的交易行里 |
| C4-17 | 日内报告里每条 `Sts/Cd=PDNG` 的明细，其 `EndToEndId` 必须在当日对账单里以 `Sts/Cd=BOOK` 出现 | **仅当不存在同 `EndToEndId` 且 `TxSts=RJCT` 的 `pacs.002` 时**。契约 P5-1 与 P5a-1。**适用条件由第三张报文决定，见下**。**依据已于 2026-09-12 由 CMOP 升级为规范**：`PDNG` 的码值定义写着 "If booking takes place, the entry will be included with status Booked in subsequent account report or statement"，契约 §4B.14.3 |
| C4-18 | `camt.053` 里**不得**出现 `Sts/Cd` 非 `BOOK` 的明细 | 全部 `camt.053`。契约 P5-2。**依据是 scope 而不是约束**，见契约 §4B.12 |
| C4-19 | 从 `Ntry` 到 `Refs/EndToEndId` 的五层路径逐层存在 | 全部 `camt.052` 与 `camt.053`。契约 P5-3。**结构存在性判据，规范一层都不要求** |
| C4-20 | **不得断言**未入账明细的 `BookgDt` 等于入账后的 `BookgDt` | 全部 P-5。契约 P5-4。**同一元素在两个状态下语义不同** |
| C4-21 | `Avlbty` 与 `ValDt` 不同时出现 | 全部明细。契约 P5-5。**散文写 must not，31 条约束里没有对应条目** |
| C4-22 | 内部账当日未入账金额等于日内报告 `PDNG` 明细有符号金额之和 | 全部 P-5。契约 P5-6。**这是三方对账的第三方** |
| C4-23 | 由请求触发的报告，其 `OrgnlBizQry/MsgId` 命中一条 `camt.060` 的 `GrpHdr/MsgId` | **仅 P-7**。契约 §4C.10.4。**回执侧该块是 0..1，规范不要求配对** |
| C4-24 | `ReqdMsgNmId` 的取值是本项目实际生成的三个报文标识符之一 | 全部 `camt.060`。契约 §4C.10.2。**规范把它做成 `Max35Text`，拼错一字完全合法** |
| C4-25 | `ReqdTxTp` 出现时，回执明细的状态与方向与请求一致 | **仅带过滤的 P-7**。契约 §4C.10.3 |
| C4-26 | 日内报告的 `PDNG` 明细未在对账单出现时，必须存在同 `EndToEndId` 的 `RJCT` | 全部 P-5a。契约 P5a-1 |
| C4-27 | 被拒的那一笔**不得**出现在对账单上，任何状态都不行 | 全部 P-5a。契约 P5a-2。**否定式，见 §1A.3** |
| C4-28 | 被拒的那一笔**不得**产生 `RvslInd=true` 的明细 | 全部 P-5a。契约 P5a-3。**否定式，见 §1A.3**。**依据已升级为规范**：`Status Pending cannot be reversed`，且 `Status Booked is the only status that can be reversed` |
| C4-29 | `RJCT` 的时点晚于该笔所在日内报告的 `CreDtTm` | 全部 P-5a。契约 P5a-4 |
| C4-30 | 内部账当日未入账金额扣除被拒的那一笔 | 全部 P-5a。契约 P5a-6。**C4-22 在 P-5a 下的修正** |
| C4-31 | `FUTR` 明细的 `ValDt` 晚于其 `BookgDt` | 全部 P-5b。契约 P5b-1 |
| C4-32 | `FUTR` 明细转 `BOOK` 后 `BookgDt` 不变 | 全部 P-5b。契约 P5b-2。**与 C4-20 方向相反**，同一元素在两个场景下一个准变一个禁变 |
| C4-33 | `FUTR` 明细在其 `ValDt` 当日以 `BOOK` 出现 | 全部 P-5b。契约 P5b-3 |
| C4-34 | `CLBD` 减 `OPBD` 只对 `Sts/Cd=BOOK` 的分录求和 | **凡当日含 `FUTR` 分录**。契约 P5b-4。**C4-8 与 P4-1 的限定版** |
| C4-35 | `FUTR` 明细**不得**带 `RvslInd=true` | 全部。**规范**：只有 `BOOK` 可冲正。契约 P5b-5 |
| C4-36 | 一笔**不得**在 `ValDt` 之前由 `FUTR` 转 `PDNG` | 全部。**CMOP**，词表没说不行。契约 P5b-6 |
| C4-37 | 内部账未起息科目余额 = 全部 `FUTR` 明细有符号金额之和 | 全部 P-5b。契约 P5b-7 |
| C4-38 | `INFO` 分录**不得**进入任何一侧的余额或内部账 | 全部。**规范**：`INFO` 的定义说未做任何入账 |

**C4-39 是本层唯一能发现"整整一天的对账单丢了"的余额判据。**
单日自洽的检查对一份缺失的报文一律沉默，**因为没有报文就没有人去判它**。
跨日等式把缺失变成不等式。

**C4-34 是本层第一条"给既有等式加限定条件"的检查。**
**2026-09-12 同日补记：这个限定不是补丁。** `CLBD` 的码值定义本来就只对已入账分录求和，
**C4-8 的无条件写法从一开始就是错读**，只是此前数据里没有非 `BOOK` 分录，错误不可见。
C4-8 与 P4-1 此前是无条件的，**`FUTR` 的存在让它们在 P-5b 下必然失败**。
**处理办法不是给 C4-8 打补丁，是并列一条限定版**：
**原式保留，用来在不含 `FUTR` 的日子上守住更强的口径。**

**C4-32 与 C4-20 必须并排读。** 两条都管 `BookgDt`，一条说允许变，一条说禁止变，
**区别只在这条分录的状态是 `PDNG` 还是 `FUTR`**。
**取错状态就取错判据**，而两个状态的字面差别只有四个字母。

#### C4-17 的适用条件在第三张报文上，这是本层第一次

**此前判据的适用条件有三种来源**：无条件、取自被判报文自身的字段、
取自同一链上的另一张报文。**C4-17 是第四种：条件取自第三张报文，而那张报文可以不存在。**

**"没有 `RJCT`"有两种含义**：没被拒，或者被拒了但没回报状态。
**规范不要求发状态回报**（契约 §4C.4），**所以两者在数据上无法区分**。
**因此 C4-26 与 C3-3 必须并存**：前者判"消失得有理由"，
后者判"CMOP 自己没漏发状态回报"。**只留 C4-26，生成器少发一条 `pacs.002`
就能让任意一笔凭空消失且判不出来。**

**C4-23 至 C4-25 在 P-7 冻结前登记为 not evaluated**，理由与 C4-5 当初相同：
**没有被测数据的断言等于没有验证过**。它们要等日内报告生成器就位。

**C4-19 是这一级里第一条结构存在性判据。** 此前三种形态是等式、引用链可达性、
状态停留时长（§2B.2 与契约 §3A.14）。**这一条把检查形态从三种扩到四种，见 §1A.1。****它要挡的是生成器少填一层，
而少填一层在规范眼里完全合法**：`EntryTransaction16` 的 29 个元素没有一个必填。

**C4-18 与 C4-20 是否定式。** 全部否定式已归入 §1A.3 的清单，此处不再逐次计数。
**它们共同要挡的不是坏数据，是一个看上去合理的写法。**

**C4-16 是这一层第一次出现锚点粒度退化。**
此前所有跨段判据都是逐笔的，**整组退汇逼出一个逐批的**。
代价不是判不了，**是判错之后定位不到笔**——
而"定位到具体事件"正是代表性 BQ 要的东西，见[业务目标](business-objectives.md) §3.2。
**这一环做不到，必须写明，不能靠沉默蒙混。**

**C1-11 是清单里第一条"写下来是为了记住它错在哪"的条目。**
它此前作为规范要求存在过，**依据是 MDR 散文**；读形式化才发现多一个条件。
**删掉它比留着更危险**：不留痕迹的话，下一个读散文的人会把它原样加回来。

**C1-14 挡的是一处"可选元素带走整组约束"。**
`GrpRtr` 是 0..1，缺席时管整组退汇的五条约束**全部不触发**，
于是一条声称五笔而一个交易行都没有的退汇报文完全合法。
**这是本项目第三次遇到这个形状**，前两次是零必填的状态回报与结算状态轴。
**三次的共同点是：约束挂在可选元素上，元素缺席则约束失效，而 schema 一声不吭。**

**C2-25 的形状值得单独指出。** 它禁止的不是一个错值，**是一个在别处完全正当的元素**：
`pacs.004` 可以带底层划拨，只是这一笔不能带。**这类条件性禁止在 schema 上一律看不出来**，
而生成器最容易在"能填就填"的思路下踩中。

### 2C.5 C3：同一 `EndToEndId` 的报文集合

**关联键有三层，深度与必填性都不同**，见契约 §4B.2：

| 层 | 路径 | 必填性 |
|---|---|---|
| 报文层 | `GrpHdr/MsgId` ← `OrgnlGrpInf/OrgnlMsgId` | 两端都必填 |
| 交易层 | `PmtId/EndToEndId` ← `TxInfAndSts/OrgnlEndToEndId` | **发起端必填，回报端不必填** |
| 跨段层 | `UndrlygAllcn/RltdRefs/SctiesSttlmTxId` | **两端都可选** |

**第二层的不对称是本节要反复用到的事实**：`pacs.009` 一定带 `EndToEndId`，
**`pacs.002` 不一定带 `OrgnlEndToEndId`**。**因此"每条状态回报都能回连"是 CMOP 约束，
不是规范保证的性质**，C2-9 已经把它前移到单行级。

| 编号 | 检查 | 适用条件 |
|---|---|---|
| C3-1 | 每条 `pacs.002` 交易行都能回连到一条存在的 `pacs.009` 交易行 | 全部 |
| C3-2 | `OrgnlMsgId` 与 `OrgnlMsgNmId` 与被回引报文一致 | 全部。两者规范必填，**因此这条能真正校验到东西** |
| C3-3 | 同一 `EndToEndId` 下 `pacs.002` 至少一条 | **仅 CMOP 数据内**。规范说状态回报在每个流程里都是可选的（契约 §4C.4），**这条不能拿去校验外部报文** |
| C3-4 | 同一 `EndToEndId` 下状态时点严格递增 | 全部 |
| C3-5 | 状态序列不回退：`ACTC` 之后不再出现 `RCVD`，`ACCC` 之后不再出现任何非终态 | **仅 P-1 与 P-2**。**退汇与冲正会破坏它**，见 §2C.6 |
| C3-6 | `FctvIntrBkSttlmDt` 不早于 `IntrBkSttlmDt` | 全部资金场景。**P-1 下取等，P-2 下严格大于** |

**C3-3 的写法是本节从 MDR 直接得到的一条修正。** 它原先在契约 §4B.4 里记为 P1-8，
适用条件写的是"所有资金场景"。**读完支付 MDR 后收窄为生成侧的自我约定**，
因为规范明说状态回报可选。**一条不发状态回报的真实支付流是合规的**，
拿 C3-3 去判它违规是本层越界。

**C3-5 是本节最容易写过头的一条。** 状态回退在退汇（`pacs.004`）与冲正（`pacs.007`）下是
正常业务，**把它写成无条件不变量，等于在 P-3 落地当天给自己造一批假阳性**。
这是 §2B.1 已经记过三次的同一失效方式，**第四次**。

### 2C.6 C4：资金段与证券段之间

**这一级判定的是六段链条是否闭合**，与 §2A.5 的"场景契约"不是一回事。

| 编号 | 检查 | 适用条件 |
|---|---|---|
| C4-1 | 每条 `UndrlygAllcn/RltdRefs/SctiesSttlmTxId` 恰好命中一条 `sese.023` 的 `TxId` | **仅 DVP**。契约 P1-4，**这是链条闭合的判据本身** |
| C4-2 | `IntrBkSttlmDt` 等于证券段同笔的 `SttlmDt` | **仅 DVP**。自由交割下两腿日期可以不同 |
| C4-3 | 款腿与券腿的借贷方向相反 | 仅 DVP。契约 P1-5 |
| C4-4 | 一条 `pacs.009` 交易行的金额等于其覆盖的各账户 `AllocNetMoney` 之和 | 仅 DVP，且该块全部账户都已结算 |
| C4-5 | 券腿失败时款腿不得出现 `ACCC` | **仅 S-2 与 P-2**。DVP 的定义要求两腿同生共死 |
| C4-6 | `FctvIntrBkSttlmDt` 等于券腿同笔的 `FctvSttlmDt` | **仅 DVP**。契约 P1-9。**P-1 下两者都等于预定日，P-2 下两者都晚于预定日且彼此相等** |
| C4-7 | 券腿处于挂起态的时段内，款腿不得处于终态 | **仅 S-2 与 P-2**。**这是 C4-5 的时段版本**，C4-5 只看终值 |

**C4-5 已于 2026-09-12 转为可检验，先前的 not evaluated 登记撤销。**
P-2 冻结后（契约 §4B.8）S-2 有了配对的资金链，**这条检查第一次有被测数据**。
**与它同时可检验的还有 C4-6**，后者是 P-2 带出的新断言。

**C4-6 与 C4-2 必须并存，不能合并。** C4-2 比的是**指示的**结算日，
C4-7 比的是**实际的**结算日，**而 P-2 存在的全部意义就是让这两组日期分离**。
**只留一条，延迟场景要么永远测不到，要么被误判为违规。**

#### C4 级里属于对账单的五条

**它们与前七条不同：前七条比的是券腿与款腿，这五条比的是对账单与资金段。**
出处见契约 §4B.9。

| 编号 | 检查 | 适用条件 |
|---|---|---|
| C4-8 | `CLBD` 减 `OPBD` 等于当日全部 `Ntry` 的有符号金额之和 | 全部 `camt.053`。契约 P4-1。**依据已由 CMOP 升级为规范**：`CLBD` 的码值定义原话是 "all entries booked"，**所以本式只在当日全为 `BOOK` 时成立**，一般形式是 C4-34，契约 §4B.15.1 |
| C4-39 | 今日 `OPBD` 等于前一营业日 `CLBD` | 凡存在前一营业日对账单。**规范**：`OPBD` 与 `PRCD` 的定义各说一遍。契约 P4-2。**本层第一条跨日等式** |
| C4-40 | 报文带 `PRCD` 时其值等于 `OPBD` | 仅当 `PRCD` 出现。**规范**。契约 P4-3 |
| C4-41 | 连续营业日的 `camt.053` 不缺日 | 全部。**CMOP**，靠营业日历。契约 P4-4。**与 C4-39 并存**：后者只能报余额不平，报不出少了一份报文 |
| C4-42 | 日内报告的 `XPCD` 减 `ITBD` 等于其 `PDNG` 明细有符号金额之和，且等于内部账当日未入账金额 | 全部 P-5。契约 §4B.15.5。**把 C4-22 从两方改成真正的三方**：一条算路只用报文，一条用内部账 |
| C4-9 | 每条 `Ntry` 的 `Amt` 等于其 `TxDtls` 各笔金额之和 | **仅在 `TxDtls` 出现时**。契约 P4-2 |
| C4-10 | 每条 `TxDtls/Refs/EndToEndId` 恰好命中一条 `pacs.009` 交易行 | 全部。契约 P4-3 |
| C4-11 | `Btch/NbOfTxs` 等于该 `Ntry` 下 `TxDtls` 的实际条数 | 仅批量明细。契约 P4-4，**与 C1-2 是同一类错误的第二次出现** |
| C4-12 | 当日 `Ntry` 覆盖当日全部已结算的 `pacs.009` 交易行，不多不少 | 全部。契约 P4-5 |

**C4-8 是这五条里唯一一条只用对账单自身就能判定的。** 因此**它单独成一个批次，
可以在资金段数据尚未到齐时先跑**。这不是实现上的优化，**是对账口径的一部分**：
真实运营里银行对账单与内部账不同时到达，**能先判定的部分就应当先判定**。

**这五条一条都不按条数对账。** 一条明细可以是二十笔支付的批量（契约 §4C.5），
**"当日明细条数等于当日支付笔数"在规范上就是错的**，写进来会在第一批带批量的
数据上全面误报。C4-11 比的是 `NbOfTxs` 与 `TxDtls` 条数，**这两者都在明细内部**，
与支付笔数无关。

### 2C.7 词表成员资格：一条正交维度，且必须带版本

~~**这是资金段独有的一类检查，结算段一条都没有。**~~
**已于 2026-09-12 推翻：结算段现在有四条**（V2-11、V2-12，以及公司行为侧的两条），
见 §2A.4 与下面第五句。

**判定规则由三句增至五句**，第四句是分发格式（见下），**第五句是码集名**。
**第五句的证据在 2026-09-12 当日增至五处，最强的是 `ACSC`**：
组级码集说它是"借方账户结算完成"，笔级码集说它是无侧别的"结算完成"，
**同一族、同一概念、只差码集名一个词**，而本项目曾把组级定义抄给笔级字段（契约 §4B.15.4）。
其余四处是 `BOOK`、`UPAY`、`DSET`，
以及 `WITH` 的第三种含义（结算指令的处理次序，"与被链接指令一同执行"，
契约 §4D.11.4）。**四处里有两处出现在同一天读的两个族里。**

**判定规则三句话：**

1. **取值必须出现在对应 `External*` 词表中**，词表不在报文 schema 里，来自单独发布的
   External Code Sets。
2. **只有 `Status` 为 `Registered` 的码值算合法。** 全份 3347 条里 **3314 条 `Registered`、
   33 条 `Obsolete`**，本日按 XLSX 逐行数过。**33 条在 XSD 里是普通 enumeration，
   没有任何形式上的区别**（契约 §4A.9），**只按 enumeration 判定会把它们全部放行**。
3. **每一条判定结果必须携带词表版本与分发格式。** 当前为 `2Q2026_externalcodesets_v3`
   的 **XLSX** 一份按 `Status=Registered` 过滤，银行交易码组合表为 `BTC_Codification_30Nov2025`。

**第三句是本层与其余各层最本质的区别：同一条报文，换一个词表版本，结论可以不同。**
一条今天合法的码值，下一季度可能被标为废止。**因此"该报文通过资金段校验"这句话
在不写版本时是没有意义的**，与 §5 里 FINTRAC 规则版本的问题同构。

**第三句还不够，2026-09-12 补上第四句：判定结果必须同时携带词表的分发格式。**

**同一次发布的三种分发格式，内容并不完全相同。** 本日把 `2Q2026_externalcodesets_v3`
的 XSD、JSON、XLSX 三份逐码对过：

| 分发格式 | 覆盖 | 带 `Status` 列 |
|---|---|---|
| XSD | **全部 3347 条，含 33 条 `Obsolete`** | 否 |
| JSON | **3314 条，恰好等于全部 `Registered`** | 否 |
| XLSX | 全部 3347 条 | **是，且只有它有** |

**逐条核对的结论是硬的：140 个带枚举的词表，JSON 的成员集合与 XLSX 里
`Status=Registered` 的集合逐码相等，一个不多一个不少。** 三个词表在 XSD 与 JSON 之间
的差异（`ExternalLocalInstrument1Code` 差 24 个、`ExternalClearingSystemIdentification1Code`
差 4 个、`ExternalCashClearingSystem1Code` 差 1 个），**差的就是那些废止码**。

**因此判定用的词表不能取 XSD。** XSD 是超集，**而它多出来的恰好是本层规则第 2 条
要求拒绝的那一批**。"用超集不会误伤合规数据"这个直觉在别处成立，**在这里正好反过来**：
拿 XSD 建判定表，等于把废止码全部放行，**本层规则第 2 条就白写了**。

#### 第五句：每一条判定必须写明码集名，不能只写码字母

**2026-09-12 补上。** 触发它的是三处同名不同物，全部在同一天遇到：

| 码字母 | 出现处一 | 出现处二 |
|---|---|---|
| `BOOK` | `ExternalEntryStatus1Code`，已入账 | `ClearingChannel2Code`，行内转账（契约 §4B.12） |
| `DSET`、`DCAN` 等 | `DeniedReason6Code`，撤销请求被拒办 | `DeniedReason4Code`，**名字不同定义不同**（契约 §4.6.6） |
| `UPAY` | `ExternalReturnReason1Code`，整组退汇 | `CorporateActionReversalReason3Code`，不应支付（契约 §4D.5） |

**因此"该字段的取值是一个合法的 `UPAY`"这句话没有意义。**
判定表必须以 `(码集名, 码值)` 为键，**而不是以码值为键**。
**这与第三句的版本要求是同一类问题**：判定结果不携带足够的限定词，结论就不可复现。

**取用口径定为：判定表由 XLSX 按 `Status=Registered` 过滤生成，JSON 作为交叉校验。**
两者应当逐码相等，**不相等即说明这一版发布本身有问题，停下来查，不要挑一个用**。
**XSD 仍然保留**，它是 XML 校验器实际会加载的那份，
**但它的角色是"schema 合法性"，不是"业务合法性"**——这正是 §4.3 那条区分在词表上的再现。

#### 一个码值被回收再用，全份里只有一个

`ExternalLocalInstrument1Code` 的 `CTP` 在 XLSX 里**有两行**：

| 名称 | 状态 | 创建 | 最后更新 | 提出方 |
|---|---|---|---|---|
| CustomerTransferPlus | **Obsolete** | 2009-04-01 | 2018-11-01 | CR0761/FED |
| CreditTransferPreferred | **Registered** | 2021-11-30 | 2024-02-28 | CR1064/European Payments Council |

**同一个码值，两个不同的含义，中间隔了三年。** 全份 3347 条里**只有这一处**，
逐对核对过。

**它的后果不在校验，在历史数据。** 一条 2015 年的报文里的 `CTP` 与一条 2024 年的报文里的
`CTP`，**字符串相同、含义不同**，而 CMOP 的词表判定只看字符串。
**所以词表的键不是码值，是"码值加生效区间"**，与账户维度做 SCD Type 2 是同一个道理。

**本项目不需要立刻实现它**：CMOP 不生成 `CTP`，也不生成跨越 2018 年的历史。
**但这条要写下来，因为它是"词表是维度不是枚举"这个论点唯一的实证**，
而那个论点决定了参考数据该放在哪一层。

#### 读 XLSX 的一个坑，落在 Bronze 摄取上

**XLSX 不为空单元格写 `<c>` 元素。** 按出现顺序取值、不看 `r` 属性里的列号，
**整行就会左移**，而且移得很安静：本日第一次解析时 `Status` 一列里出现了
132 个 `2022-11-21`、33 个 `2022-03-24` 这样的日期，**看上去像"状态字段里混进了日期"
这类脏数据问题，实际是解析器的错**。

**登记为参考数据摄取的一条硬要求：电子表格必须按列坐标取值，不得按出现次序取值。**
**这类差错的特征是它产出的数据完全可读**，只是每一行都错位，
**下游任何校验都发现不了，因为每个字段单独看都合法**。

**取用口径定死两条：**

- **生成器的取值池与本层的判定表必须来自同一份词表快照。** 两边版本不同会产生一种极难查的
  差错：**数据合法，校验说不合法，而两边都没错**。契约 §4A.9 已写，此处重申因为它落在本层。
- **词表升版是一次需要重跑的事件，不是一次配置更新。** 重跑口径见 §4。

### 2C.8 本层不做什么

- **`camt` 的条数对账一律不做。** 一条明细可以是 `Btch`、覆盖二十笔支付（契约 §4C.5），
  **按条数配平在规范上就是错的**。P-4 的对账走 `TxDtls` 层的 `EndToEndId`，
  **走金额与笔数合计，不走行数**。
- **退汇与冲正的状态语义不做。** `pacs.004` 与 `pacs.007` 属 P-3，未解锁。
  C3-5 的适用条件已标明它们会破坏状态单调性。
- **`camt.052` 与 `camt.054` 只做 C2 级检查。** 三张报表共用 `ReportEntry16`，
  明细检查可以复用；**但只有 `camt.053` 必须带余额，因此只有它能作余额对账的基准**。
- **收付双方的外部一致性不做。** 与 §2A.6 末条同理，那是 R3、R4 的事。
- **`pacs.008` 一条不做。** P-1 选的是 `pacs.009`，客户版不在已冻结场景内。
  它多出的三个必填项（`ChrgBr`、`DbtrAgt`、`CdtrAgt`）届时要单写一组 C2 检查。

### 2C.9 与故障注入的对应

**与 §2A.7 同样的规则：每条跨段检查都要有一条注入证明它有效。**
**完整清单见 §6.5**，共十条。其中前两条特意配成一对，
它们证明的是 §2C.2 里那条分级依据：**爆炸半径不同的两类违规必须落进不同的队列**。
一条注入证明检查有效，**两条配对才证明分级有效**。

## 2D. 公司行为段的断言

**2026-09-12 新建。** 出处见[监管数据契约](regulatory-data-contracts.md) §4D。
**这一段与前三段最大的不同：规范几乎不管它。**

`seev.039` 事件撤销通告全部 14 条约束都是通用校验，**零形式化**，
**撤销一个已公告事件的后果，规范一个字都没写**（契约 §4D.7）。
**因此本节几乎每一条都标 CMOP，而不是像 §2C 那样多数标规范。**
**这个比例本身要写进对外口径**：说"公司行为段通过校验"时，
**通过的是本项目自己的判据，不是 ISO 20022 的**。

| 编号 | 检查 | 依据与适用条件 |
|---|---|---|
| E-1 | 每条 `seev.039` 的 `CorpActnEvtId` 命中一条此前发出的公司行为公告 | **规范**：该字段 1..1，是本项目唯一的强锚点。契约 S7b-1 |
| E-2 | 撤销后，该事件在受影响账户上的数量与成本基础复原 | **CMOP**。契约 S7b-2。**§7 第 3 条的反向** |
| E-3 | 依赖该事件的已发布结果被重述，且旧版本保留 | **CMOP**。契约 S7b-3。**与 A-3 的验收判据同形，两条路径共用** |
| E-4 | `CxlRsnCd=WITH` 之后不得再出现同一 `CorpActnEvtId` 的新公告 | **CMOP**。契约 S7b-4。**`PROC` 下这条不适用** |
| E-5 | 撤销的作用范围按账户判定 | **规范使之必要**：`AcctsDtls` 允许 `AcctsList`。契约 S7b-5 |
| E-6 | **不得断言**撤销之后该事件从未存在 | **CMOP**。契约 S7b-6。**否定式，见 §1A.3** |
| E-7 | `EvtCmpltnsSts` 与 `EvtConfSts` 的组合限于 `WITH`+`CONF` 与 `PROC`+`UCON` | **CMOP**。规范允许四种组合，本项目只生成两种 |
| E-8 | `seev.037` 的 `RvslRsn/Rsn/Cd` 落在 `CorporateActionReversalReason3Code` 内 | **规范**。**必须写码集名**，`UPAY` 在另一码集里是退汇原因，见 §2C.7 第五句 |
| E-9 | `seev.037` 冲正的是一笔过账，**不得**触发调整因子回退 | **CMOP**。契约 §4D.4。**这是 E-2 的反面，两条必须并存** |
| E-10 | `seev.036/MvmntConfId` 必填 | **CMOP**。规范是 0..1，只有分页时才强制（C49）。契约 §4D.10 |
| E-11 | 每条 `seev.037` 的 `MvmntConfId` 命中一条 `seev.036` 的 | **规范只保证指针必填，不保证目标存在**。契约 S7a-1 |
| E-12 | 冲正与被冲正过账的 `FinInstrmId`、`PstngQty`、`CdtDbtInd` 一致 | **CMOP**，规范无约束。契约 S7a-2 |
| E-13 | `seev.037/OrgnlPstngDt` 等于被冲正过账的 `PstngDt` | **CMOP**，规范是 0..1。契约 S7a-3 |
| E-14 | 账户持仓在冲正后回到过账前的数量 | **CMOP**。契约 S7a-5。**账户级，不是过账级** |
| E-15 | 冲正之后该事件仍然存在且仍然确认 | **CMOP**。契约 S7a-6。**与 E-9 是同一件事的两面，两条都留** |
| E-16 | `CANT` 撤销必须有一条回指它的重发指令 | **CMOP**，规范无约束。契约 S7c-1 |
| E-17 | `CORP` 撤销**不得**有回指它的重发指令 | **CMOP**。契约 S7c-2。**否定式，与 E-16 互为反面** |
| E-18 | 重发指令的 `Lnkgs/Ref` 走 `SctiesSttlmTxId` 分支 | 仅当 `Lnkgs` 出现。**七选一，填错分支 XSD 全部沉默**。契约 S7c-3 |
| E-19 | 重发指令的 `Lnkgs/PrcgPos/Cd` 为 `INFO` | 仅当 `PrcgPos` 出现。**依据是 C80**，其余三值按规范会把重发指令永久挂起。契约 S7c-4 |
| E-20 | 撤销与重发落在同一 `CorpActnEvtId` 之下 | 全部。**报文层面不可验证**，靠生成侧对照表。契约 S7c-5 |
| E-21 | **不得断言**能从 `sese.027` 的 `Canc` 分支读出 `CANT` | 全部。**否定式**，`CancelledStatusReason5Code` 只有两个值。契约 S7c-6 |
| E-22 | 每条 `seev.036` 的 `OptnNb` 命中该事件 `seev.031` 里的某个 `OptnNb` | **规范**：`seev.033` C20 要求按编号区分。**`OptnNb=UNSO` 时不适用**。契约 S8-1 |
| E-23 | 同一事件的 `seev.031` 里 `OptnNb` 互不相同 | **规范在指令侧是 must、通知侧是 recommended**（C20 与 C88），按强的判。契约 S8-2 |
| E-24 | `CHOS` 事件恰好一个选项 `DfltOptnInd=true`；`VOLU` 事件零个 | **CMOP**，规范不管数量，C12 只管默认与已应用不得并存。契约 S8-3 |
| E-25 | `CHOS` 与 `VOLU` 事件的选项带 `RspnDdln` 与 `MktDdln`，且前者早于后者 | **CMOP**，规范两者皆 0..1。契约 §4D.12.4 |
| E-26 | 截止前有被接受的指令时，确认的 `OptnNb` 等于该指令的 `OptnNb` | 仅当存在 `AccptdForFrthrPrcg`。契约 S8-4 |
| E-27 | 截止前没有被接受的指令时，确认的 `OptnNb` 等于默认选项，且存在一条 `DfltActn` | 仅 `CHOS`。**依据是 `CHOS` 的码值定义**。契约 S8-5 |
| E-28 | 选现金：持仓减少 = 指令数量，现金增加 = 数量 × 公布价；选证券：§7 第 3 条**按该选项比率单独求值** | 按选项分别适用。契约 S8-6、S8-7 |
| E-29 | 被拒指令原因码为 `ADEA` 时时点晚于 `RspnDdln`，为 `LATE` 时晚于 `MktDdln` | 仅被拒指令。**`ADEA` 之后的处理不断言**，定义写的是按双边协议。契约 S8-8 |
| E-30 | **不得断言**通知、指令、确认三者的 `OptnTp` 相等 | 全部。**否定式**，三个码集 22／26／23 个值。契约 S8-9 |
| E-31 | **不得要求** `DfltActn` 状态回报带 `InstrId` | 全部。**否定式**。契约 S8-10 |

**E-22 至 E-31 是 2026-09-13 选择权事件纳入范围后补的**，契约 §4D.12。
**这十条里依据规范的比例明显高于本节其余各条**：`seev.033` 与 `seev.034` 的约束
比 `seev.039` 实在得多。**E-27 与 E-29 合起来区分"没说"与"说晚了"**，
两者结果相同、责任方不同，这是 T+1 早晨异常处理要回答的问题。

**E-19 是本节唯一一条"依据规范却在拦一个规范允许的写法"的检查。**
C80 不禁止用 `WITH` 链接一条已撤销的指令，**它只是规定了后果**：
被链接的指令执行不了，全组保持挂起。**而被撤销的那条永远执行不了。**
**所以合规地写下 `WITH`，得到的是一条永远不结算的重发指令**，
数据上完全合法，业务上是一笔丢失的交易。**这类"合规即错误"的检查此前一条都没有。**

**E-20 的地位与本节其余各条不同：它判不了。** `sese.020` 与 `sese.023` 都不带
事件标识，**这条只能拿 CMOP 自己的生成记录来核**。
**写进清单是为了让"判不了"这件事留在明面上**，
而不是让下一个人以为它是从报文里判出来的。

**E-11 是"指针必填而目标可选"这一形状的第一条检查。** 此前遇到的四种锚点形状
（契约 §4.6.6 的表）说的都是指针本身强不强，**这一种两头合规却仍然接不上**。

**E-2 与 E-9 是本节的核心配对。** 两张报文长得像、名字里都有"冲正／撤销"，
**下游后果却完全相反**：一张要回退整个事件的调整，一张只改一笔过账。
**只写 E-2，生成器把 `seev.037` 当成 `seev.039` 处理也查不出来**，
而那会把一次记账更正放大成一次全账户重述。

**E-1 是本节唯一一条依据规范的存在性检查**，因为 `CorpActnEvtId` 是必填的。
**与 §2C.6 的 C4-19 对照**：那里锚点五层全可选，这里锚点必填。
**两者写法不同：C4-19 要先断言存在，E-1 直接断言命中。**

### 2D.1 本节不做什么

- ~~**不做 `seev.031` 与 `seev.033`。**~~ **2026-09-13 纳入范围**，见 E-22 至 E-31，契约 §4D.12。
- **不做改指令、撤回、`UNSO`、按比例分配与超额认购。** 理由见契约 §4D.12.9，改指令登记为 S-8a。
- **不做部分撤销的正常路径。** `AcctsList` 只作注入，E-5 是为它写的。
- **不做 COAF 一致性。** 本项目不生成 COAF，契约 §4D.1。

## 3. 差异分类

**每一条差异必须落入一个类别，"未分类"本身是一个类别并且必须被计数。** 把未分类的差异
隐藏起来，等于把最危险的那部分从视野里去掉。

| 类别 | 含义 | 典型来源 |
|---|---|---|
| 时点差异 | 两侧都对，只是尚未到达对方的时点 | T+1 结算错位 |
| 迟到 | 应到未到 | 迟到确认、迟到对账单 |
| 数量或金额不一致 | 两侧都有，值不同 | 更正、拆分成交 |
| 口径性差异 | 两侧按不同定义计算 | 两个官方汇率来源 |
| 重复 | 同一事件被计入多次 | 重投、重跑未幂等 |
| 缺失 | 一侧完全没有 | 漏发、过滤错误 |
| 未分类 | 不属于以上任何一类 | 必须计数并单独告警 |

**时点差异与迟到必须分开。** 前者是正常的，会在下一批自然消失；后者是异常的，需要人处理。
把两者合并会让异常队列里充满噪声，运营分析员就会开始忽略它，那时整个 BO 就失效了。

## 4. 可重跑验证

三条断言，强度递增：

1. **无错重跑**：批次重跑不报错。这是最弱的一条，单独成立没有意义。
2. **结果幂等**：同一批次重跑后，目标表的内容与首次运行**逐行相同**。用输出行的稳定哈希
   比对，不用行数或汇总值，后者会掩盖抵消性错误。
3. **种子可复现**：同一根种子、同一生成器版本、同一参考数据版本，产出完全相同，见
   [生成规范](data-generation-specification.md) §2。

**第 2 条必须在有迟到数据的批次上验证。** 在干净批次上验证幂等是没有信息量的：`MERGE INTO`
的正确性恰恰体现在有更新要写的时候。

## 5. 验收门禁

**门禁结果是三分类：pass、fail、not evaluated。**

**`not evaluated` 绝不能计为 pass。** 一项检查没跑，与它跑了并通过，是完全不同的两件事。
把两者混同是数据质量框架最常见也最致命的缺陷，因为它让缺失的覆盖面看起来像通过的覆盖面。

- 门禁**默认失败**。检查未运行、结果未回写、或状态无法判定时，一律记为 fail 或
  not evaluated，不得因缺少证据而放行。
- 每次门禁结果必须记录：检查标识、规则版本、数据版本、运行时间、结论。
- 门禁结果不得只存在于日志文本里，必须是可查询的结构化结果，见
  [平台架构](../platform-architecture.md) §9。

**新鲜度是门禁的一部分。** 批次未在[工作负载基线](../workload-baseline.md) §5.2 规定的窗口
内完成，即使所有数据检查通过也不算通过，因为异常队列没能在分析员到岗前就位。

## 6. 故障注入

**注入的目的是证明检查有效，不是证明系统能崩。** 每一类注入必须写明四项：注入位置、注入
比例、预期被哪条检查捕获、正确处理后的预期结果。缺少第四项的注入不是测试。

不依赖 FIX 证据、现在即可确定的注入类别：

| 注入 | 预期捕获者 |
|---|---|
| 批次迟到到达 | 时点差异与迟到的分类边界 |
| 同一批次重复投递 | 幂等断言与重复类别 |
| 整批缺失 | 缺失类别与门禁默认失败 |
| 反向合股后的持仓重述 | [生成规范](data-generation-specification.md) §7 第 3 条不变量 |
| 参考数据序列拼接边界的重复与缺口 | 口径性差异类别 |

### 6.1 L-1 的状态机违规注入

转移表已填，见[监管与行业数据契约](regulatory-data-contracts.md) §3.4。

| 注入 | 违反 | 正确处理后的预期结果 |
|---|---|---|
| 跳过接受回报，首条即成交 | L-1 步骤序列 | 整条订单链标为契约违规，不进入正常对账 |
| 首笔成交即全部成交 | L-1 要求至少三笔 | 同上 |
| `Filled` 之后继续发送成交 | L-1 终态 | 同上 |
| 派生字段与成交序列不符 | [生成规范](data-generation-specification.md) §6.1 | 数量与均价断言捕获，归入数量或金额不一致 |
| `ExecID` 重复 | 全局唯一性 | 归入重复类别，去重后结果不变 |
| 同一订单链内 `TransactTime` 逆序 | 链内严格递增 | 归入未分类并单独告警 |

**前三条是 CMOP 场景契约违规，不是 FIX 违规。** FIX 4.4 本身允许单笔全成，规范原文也明确
提醒"矩阵未展示某转移不代表 FIX 禁止它"。把它们标成 FIX 违规是错的，而且是会传染的错：
断言里写错一次，对外口径也会跟着说错。

**L-2 落地时必须删掉，不是保留。** 单笔全成届时成为合法路径，这条注入会从有效测试变成永久
的假阳性来源。**每一条场景契约类注入都必须登记它在哪个场景扩展后失效。**

**断言必须带适用条件。** 见同一文档 §3.6：把只在订单存活期间成立的等式写成无条件不变量，
会在正常的撤单订单上批量报出假差异。假差异比漏报更致命，因为它让分析员停止相信队列。

### 6.2 注入比例：统一口径，且全部是占位值

**四段的注入清单共用一套比例档位**，避免每段各定一套。**数字是占位值**，
处理方式与[生成规范](data-generation-specification.md) §6.2 相同：
被论证的是档位划分与理由，不是具体数值，Phase 1 实测后重定。

| 档 | 比例 | 用在哪类注入 |
|---|---|---|
| 稀 | 链条总数的 0.1% | **单条链内部的字段级违规**。稀是因为它们只需被捕获一次即可证明检查有效 |
| 中 | 1% | **跨报文的关系违规**，例如回连不上、次序颠倒。要足够多才能看出它们在队列里的聚集形态 |
| 密 | 5% | **分类边界类注入**，例如迟到、重复。它们的价值在于压出分类阈值，样本太少压不出来 |

**三档的划分依据是"要证明什么"，不是"多严重"。** 一条被捕获的注入就证明了检查有效；
要证明**分类阈值**选得对，则需要足够多的样本落在边界两侧。**严重程度不进入这个决定。**

**每一条注入都必须登记它在哪个场景扩展后失效**，规则见 §6.1 末段。
**场景契约类注入在对应场景解锁后是要删掉的，不是保留的**，
否则它会从有效测试变成永久的假阳性来源。

### 6.3 分配与确认段的注入

**每条注入对应 §2B.2 的一条断言。** 清单按断言编号组织，**因为漏掉一条断言比漏掉一条注入
更难发现**——注入缺失只是少测一次，断言无注入则是它从未被验证过却一直在报结论。

| 注入 | 档 | 预期捕获者 | 正确处理后的预期结果 | 失效于 |
|---|---|---|---|---|
| 某账户 `AllocQty`(80) 改小，使各账户之和不等于 `Quantity`(53) | 稀 | VA-1 | 整条分配链标为契约违规，不进入正常对账 | — |
| `GrossTradeAmt`(381) 按净价而非毛价计算 | 稀 | VA-3 与 VC-6 **两条同时** | 同上。**两条同时命中本身是预期结果**，见下 | — |
| 卖出链上 `AllocNetMoney`(154) 按买入公式加费用 | 稀 | VA-5 | 同上，并归入金额不一致 | — |
| 引入一张跨日均价的分配指令 | 稀 | **无人捕获，这是预期** | VA-6 是否定式断言，**此注入用于证明没有人把它重新发明成正向断言** | — |
| 某账户缺确认 | 中 | VC-1 | 该账户的链标为契约违规 | — |
| ~~某账户两条确认，均为 New~~ | — | ~~VC-2~~ | **已于 2026-09-12 随 C-2 冻结删除**，该形状现在合法 | — |
| Replace 型确认不带 `ConfirmRefID` | 中 | VC-7 | 该确认链标为契约违规 | — |
| Replace 型确认只带变更字段 | 中 | VC-9 | 同上 | — |
| `AffirmStatus=2` 的应答不填 `ConfirmRejReason` | 中 | VC-8 | 同上 | — |
| `ConfirmRefID` 两条互指成环 | 稀 | VC-10 | 该链标为契约违规且**必须不进入死循环** | — |
| 一条抄送确认被回以 `AffirmStatus=3` | 稀 | VC-11 | 该确认标为契约违规 | — |
| 拒绝率报表不按 `CopyMsgIndicator` 分层 | — | VC-12 | **这不是数据注入，是口径评审项**，在报表评审时检查 | — |
| 分配落在批次 T+1，成交落在批次 T | 中 | **批次 T 的 R1 应当报差异，这是预期** | 差异归类为"缺失"；批次 T+1 后消失；**批次 T 的结果被重述且两版并存** | — |
| 上一条，但实现把批次 T 的结果原地覆盖 | 稀 | **仅 VT-10**，全部等式照样跑绿 | 重述审计失败。**这是 A-3 唯一真正的验收判据** | — |
| 确认的 Affirmed 落在批次 T+2，结算日为 T+1 | 中 | VT-9 与 S-2 的入口条件 | 该笔转入延迟结算路径，**停留时长与跨界标志分别记录** | — |
| 上一条，但停留时长与跨界标志被合成一个字段 | 稀 | **仅 VT-12** | 无法区分"久而未跨"与"短而跨界" | — |
| `PossResend=Y` 的重发件，业务字段与原件完全相同 | 中 | VT-6 | 按业务键判为重复。**序号不同，序号去重对它无效** | — |
| 上一条，但 Bronze 丢弃了会话头 | 稀 | **仅 VT-7** | 重发件与原件在 Silver 里不可区分 | — |
| `OrigSendingTime` 等于 `SendingTime` 的重传件 | 稀 | VT-3 | 记为未知延迟。**记成零会把分布拉向左侧，且每条样本单独看都合法** | — |
| 一条确认内 Σ`OrderCapacityQty`(863) 与 `AllocQty`(80) 不等 | 稀 | VC-3 | 该确认标为契约违规 | — |
| `AvgPx` 填扣费后净价，且三件套按净价自洽 | 稀 | **仅 VC-6**，等式检查全部通过 | **这是本段最重要的一条注入**，见下 | — |
| 抄送确认排在主确认被应答之前 | 稀 | VC-5 | 归入次序违规 | — |
| Replace 型分配指令只带变更字段，其余留空 | 中 | VA-10 | 该分配链标为契约违规 | — |
| Replace 型分配指令不带 `RefAllocID` | 中 | VA-7 | 同上 | — |
| `AllocStatus=0` 的应答上带出一个 `NoAllocs` 组 | 稀 | VA-9 | 同上。**正向的必填式校验对它完全静默** | — |
| `AllocStatus=2` 的应答上既不填 `AllocRejCode` 也不给 `NoAllocs` 组 | 中 | VA-8 | 同上 | — |
| `RefAllocID` 指向一条本身也是 Replace 的指令，且两者互指成环 | 稀 | VA-11 | 该链标为契约违规且**必须不进入死循环** | — |
| 账户层原因码填 `AllocRejCode` 域外的值 | 稀 | **仅 VA-14**，FIXML 校验通过 | 该应答标为契约违规。**用于证明账户层的域检查没有被当成 schema 的重复而省掉** | — |
| 一条 `AllocStatus=4`（incomplete）的合法应答 | 稀 | **无人捕获，这是预期** | VA-13 的反向注入：**若有人把合法域写回四值，这条合法数据会被误判** | — |
| Replace 之后对方回 `AllocStatus=2` 而非 `0` | 稀 | **无人捕获，这是预期** | VA-12 的反向注入，与上一条同理 | — |
| 合法的 A-2b 链：重下的 New 不带 `RefAllocID` | 中 | **无人捕获，这是预期** | VA-16 的反向注入：**若有人要求重下件回引原件，合法链会被判为孤儿** | — |
| 重下的 New 带上 `RefAllocID = A1` | 稀 | VA-17，按第二档报告 | 该分配标为偏离码值语义，**不标为违反规范** | — |
| Cancel 被接受后，R1 分配侧仍计入 A1，或把 Cancel 件当作链末端计入 | 稀 | VA-15，**R1 同时报数量两倍** | 该订单的 R1 结果作废重算。**用于证明有效性没有被写成"取链末端"** | — |
| 撤销件不带 `NoAllocs` 组 | 中 | **无人捕获，这是预期** | VA-10 只约束 Replace，**若有人把它套到 Cancel 上，合法撤销件会被判缺字段** | — |
| 重下件的 `TransactTime` 早于撤销件被接受 | 稀 | VA-18 | 生成器自检失败，**本批数据不发布** | — |
| 某账户只有 K1 与撤销件 K2，缺重下件 K3 | 中 | **仅 VC-14**，VC-1 照样跑绿 | 该账户标为无有效确认。**这是 C-2c 最重要的一条注入** | — |
| 合法的 C-2c 链：撤销件之后没有 AU | 中 | **无人捕获，这是预期** | VC-16 的反向注入之一 | — |
| C-2c 链上给撤销件补一条 `AffirmStatus=1` 的 AU | 稀 | **无人捕获，这是预期** | VC-16 的反向注入之二。**两条都要跑，双向否定只跑一边等于只守了一半** | — |
| 重下的确认带上 `ConfirmRefID = K1` | 稀 | VC-17，按第二档报告 | 同 VA-17 那条 | — |
| 确认更正对照表中重下件指向另一个账户的原件 | 稀 | VC-18 | 生成器自检失败，本批数据不发布 | — |
| 合法的 A-2b 链上跑 VA-11 | 中 | **无人捕获，这是预期** | 撤销件一跳到 A1 为 ok，**A3 零跳自身为根也是 ok** | — |

**两条要单独解释。**

**`AvgPx` 填净价那一条，是本段唯一一条等式检查全部通不过的注入。** 金额三件套会自洽地
全错，每一条单独看都对（§2B.2 末段）。**它证明的是口径类断言不可被等式类断言替代**——
如果去掉 VC-6，这条注入会完全静默通过，而数据是错的。**任何一次简化断言清单的动议，
都要先跑这一条。**

**本段现在有八条反向注入。** 跨日均价那条守 VA-6，
`AllocStatus=4` 那条守 VA-13，Replace 后再拒那条守 VA-12；
2026-09-13 随 A-2b 与 C-2c 再增五条：合法重下件守 VA-16，撤销件不带账户组守 VA-10 的作用域，
撤销件有无应答两条守 VC-16，合法 A-2b 链上的回溯守 VA-11。
**三条的共同点是：被守护的断言都是否定式或"域比看上去更宽"式，
它们的失效不表现为漏报，而表现为对合法数据的假阳性。**
正向注入永远测不到这种失效。

**跨日均价那一条是反向注入：它的预期结果是没有告警。** 注入清单里出现一条"预期不被捕获"
的条目看着奇怪，但 VA-6 是否定式断言，**它唯一可能的失效方式就是有人把它改写成正向断言**，
而那种失效只有在合法数据上报出假阳性时才暴露。**这条注入守的是断言清单本身，不是数据。**

### 6.4 结算段的注入

**§2A.7 已定的两条在此并入，不另列。** 两条都是 V3-2 的反面，属场景契约类。

| 注入 | 档 | 预期捕获者 | 正确处理后的预期结果 | 失效于 |
|---|---|---|---|---|
| 空 `<FinInstrmId/>` | 稀 | V1-023-2 | 该指令标为契约违规。**它过得了 XSD**，见契约 §4C.1 | — |
| 四个状态轴全空的 `sese.024` | 稀 | V1-024-2 | 同上，同样过得了 XSD | — |
| `Pmt=APMT` 但不带 `SttlmAmt` | 稀 | V1-023-8 | 同上。**这是具名约束 `SettlementAmountRule` 的反面** | — |
| `Pmt=APMT` 的 `sese.025` 不带 `SttldAmt` | 稀 | V1-025-4 | 该确认作废。**这是 `SettledAmountRule` 的反面**，与上一条是同一规则的两张报文 | — |
| 填 `Party1`、`Party3`，`Party2` 留空 | 稀 | V1-ALL-1 | 该报文标为契约违规 | — |
| 一条收方向 `sese.023` 不带交付方一侧的 `Dpstry` | 稀 | V1-ALL-4 | 该指令作废。**XSD 判不出来，该块 schema 上是 0..1** | — |
| 一条只带结算状态轴的 `sese.024` | 中 | **无人捕获，这是预期** | **必须判为已匹配**；若下游把匹配状态补成 NULL 并当作未匹配，即为 V1-ALL-5 未实现 | — |
| 一批外部报文的 `AcctOwnrTxId` 全填 `NONREF` | 中 | **无人捕获，这是预期** | **回连检查必须跳过它们而不是全部报错**；若报错，说明 V1-ALL-2 没被实现成豁免 | — |
| 两笔无关交易同填 `NONREF` 后按该字段连接 | 稀 | V1-ALL-2 | **必须不产生连接结果**；产生笛卡尔积即为实现缺陷 | — |
| `LongNumber` 填自定义编号而非 XML 报文标识符 | 稀 | V1-ALL-3 | 该链接标为契约违规 | — |
| `SttlmAmt/CdtDbtInd` 与 `SctiesMvmntTp` 同向 | 稀 | V1-023-8 后半 | 归入方向违规。**这是符号坑的第二次出现**，见 §2B.3 | — |
| `sese.025` 的数量与 `sese.023` 不一致 | 中 | V2-5 | 整条链标为契约违规 | — |
| 同一 `TxId` 下两条 `sese.025`，`PrtlSttlmInd=NPAR` | 中 | V2-3、V2-14 | 同上 | **2026-09-13 部分结算已解锁**：注入改为限定 `NPAR`，不删除 |
| `PrcgSts` 在同一 `TxId` 下发生变化，**且该链不属于 S-3** | 中 | V2-6 | 同上 | **已于 2026-09-12 收窄**：S-3 已冻结，注入必须排除撤销链，否则它会把合法的 S-3 数据全部报成违规 |
| S-3 链漏发第 6 步，原指令停在 `AckdAccptd` | 稀 | V3-6 | 整条链标为契约违规 | — |
| `sese.026` 的 `ConfRef` 指向不存在的确认 | 稀 | V3-11 | 该冲正标为孤儿 | — |
| `sese.026` 把 `SctiesMvmntTp` 翻转 | 稀 | V3-12 | 该冲正标为契约违规。**不翻转才是对的，这条注入防的是"看起来更像冲正"的直觉** | — |
| 被冲正的确认仍进入持仓与金额求和 | 中 | V3-15 | 聚合结果与逐笔明细对不上。**这是本段唯一一条必须在 Silver 层才能捕获的注入** | — |
| S-4 链多发一条 `PrcgSts=Canc` 的 `sese.024` | 稀 | V3-7 | 同上。**撤销被拒办却改了原指令状态** | — |
| `sese.027` 终态填 `CANS` 而非 `CANI` | 稀 | V3-8 | 同上。**撤销发起方被说成系统** | — |
| 撤销请求的时点排在结算确认之前，但两条链各自内部有序 | 稀 | V2-8 | 同上。**这条注入只有合并排序才抓得住** | — |
| 两条不同的 `sese.020` 都把 `AcctOwnrTxId` 填成 `NONREF` | 稀 | V2-9 | 两条都标为无法关联。**若未挡住，它们会互相命中，看起来是一条撤销被回报了两次** | — |
| `sese.020` 不填 `CxlRsn` | 中 | V2-10 | 该撤销标为原因缺失。**规范允许，XSD 与 25 条约束全部沉默** | — |
| `sese.027` 的 `Canc` 分支填 `CANS` | 稀 | V2-11 | 词表错配。**`CANS` 在 `CancelledStatusReason16Code` 里是合法码**，错的是分支 | — |
| `sese.027` 的 `Dnd` 分支填 `DCAL` | 稀 | V2-12 | 词表错配。**`DCAL` 属于 `DeniedReason3/7Code`，是回购专用码** | — |
| `sese.020` 给了 `TxDtls` 但省掉 `SttlmQty` | 稀 | V2-13 | 该撤销请求作废。**XSD 会拦住这一条**，注入用于证明校验层与 XSD 的结论一致 | — |
| `PrtlSttlmInd=NPAR` 的指令下出现一条 `PrtlSttlm=PAIN` 的确认 | 中 | V2-14 | 该确认标为违反指令条件 | — |
| `PART` 指令下两条确认都填 `PAIN`，没有 `PARC` 收尾 | 中 | V2-15 | 该交易标为部分结算未收尾 | — |
| `PARC` 收尾时累计结算量比指令少一百股 | 稀 | V2-16 | 标为放弃剩余而未走撤销。**按 `PARC` 码值定义这条合法**，按本项目口径不合法 | — |
| `PrvsPrtlConfId` 指向一条时点更晚的确认 | 稀 | V2-17 | 标为链反向 | — |
| `CANZ` 拆出的两条新指令数量之和比原指令多一百股 | 稀 | V2-18 | 标为拆分不守恒 | — |
| S-9b 的新指令确认带 `PrtlSttlm=PAIN` | 中 | V2-19 | 标为把拆分当成原生部分结算 | — |
| 用 `PrtlSttlmInd` 的 `PARC`（有现金下限）去满足 V2-15 的收尾判定 | 稀 | V2-20 | 标为元素错配。**同一张报文里两个 `PARC`** | — |
| `PART` 指令两条确认 `PAIN`→`PARC`，累计等于指令数量，`PrvsPrtlConfId` 正确回引 | 中 | **无人捕获，这是预期** | **V2-3 与 V2-14 的配对注入**：部分结算的正常样子。**若被报出来，说明 V2-3 仍然无条件** | — |
| S-9b 两条新指令各自一条不带 `PrtlSttlm` 的确认，合计等于原指令 | 中 | **无人捕获，这是预期** | **V2-19 的配对注入**：拆分的正常样子 | — |

### 6.4A 公司行为段的注入

**本段注入的特点与其余各段相反：多数注入在规范眼里完全合法**，
因为规范对这一段几乎没有约束（§2D）。

| 注入 | 档 | 预期捕获者 | 正确处理后的预期结果 | 失效于 |
|---|---|---|---|---|
| `seev.039` 的 `CorpActnEvtId` 指向一个从未公告的事件 | 中 | E-1 | 该撤销标为孤儿，**不触发任何回退** | — |
| 事件撤销后调整因子未回退 | 中 | E-2 | 该事件全部受影响账户标为回退缺失 | — |
| 事件撤销后已发布结果被就地覆盖而非重述 | 中 | E-3 | **与 A-3 的同名注入共用判据**，见 §6.3 | — |
| `WITH` 撤销之后重新公告同一 `CorpActnEvtId` | 稀 | E-4 | 标为事实矛盾。**`PROC` 下同样的数据必须保持沉默** | — |
| 用 `AcctsList` 只撤销部分账户 | 中 | E-5 | **按账户判定**。若判据按事件写，会把未撤销账户的持仓也回退掉 | — |
| `WITH` 配 `UCON` | 稀 | E-7 | 标为组合越界。**规范允许这个组合**，越界的是 CMOP 口径 | — |
| `seev.037` 的原因码填 `UPAY`，但取自退汇码集的定义 | 稀 | E-8 | **码集错配**。码字母相同，见 §2C.7 第五句 | — |
| 把一条 `seev.037` 当成事件撤销处理，触发全账户回退 | 中 | E-9、E-15 | 标为过度回退。**这是本段最贵的一条错误**，一次记账更正被放大成一次全账户重述。**两条应当同时报**，只报一条说明处理路径已被合并 | — |
| `seev.036` 不给 `MvmntConfId` | 中 | E-10 | 该确认标为不可回引。**规范允许，只有分页时才禁止** | — |
| `seev.037` 的 `MvmntConfId` 指向一个不存在的确认 | 中 | E-11 | 该冲正标为孤儿。**规范两头都合规**，这是本项目自己的判据 | — |
| 冲正的 `PstngQty` 比被冲正的少一股 | 稀 | E-12、E-14 | 该冲正作废。**两条都应报**：一条看报文一致性，一条看账户余额 | — |
| 冲正不填 `OrgnlPstngDt` | 中 | E-13 | 标为回引信息不足。**规范是 0..1，XSD 与 24 条约束全部沉默** | — |
| 一次真撤销，事件记录保留并打上撤销标记，历史全留 | 中 | **无人捕获，这是预期** | **E-6 的配对注入**：撤销的正确处理就是留痕，**若这条被报出来，说明 E-6 被实现成了"撤销即删除"的正向断言** | — |
| `CANT` 撤销之后没有任何重发指令 | 中 | E-16 | 标为交易凭空消失。**规范不管这一条**，`CANT` 与"是否有后继"没有任何约束绑定 | — |
| `CORP` 撤销之后又发了一条回指它的重发指令 | 中 | E-17 | 标为多出一笔。**与上一条是同一处误用的两个方向** | — |
| 重发指令的 `Lnkgs/Ref` 填 `OthrTxId` 而不是 `SctiesSttlmTxId` | 中 | E-18 | 标为分支选错。**七个分支类型全是 `Max35Text`，XSD 会保持沉默** | — |
| 重发指令的 `Lnkgs/PrcgPos/Cd` 填 `WITH` | 中 | E-19 | 标为会导致永久挂起。**这条数据完全合规**，错的是它按 C80 产生的后果 | — |
| 重发指令的 `PrcgPos` 整块不给 | 稀 | E-19 | 同上，**缺席与填错都要报**：缺席时次序关系未声明，下游各按各的默认 | — |
| 从 `sese.027` 的 `Canc` 分支回读撤销原因，期望得到 `CANT` | 稀 | E-21 | 标为口径错误。**这是一条针对处理逻辑而不是针对数据的注入** | — |
| `seev.036` 的 `OptnNb` 为 `003`，而通知只有 `001` 与 `002` | 中 | E-22 | 该确认标为选项孤儿 | — |
| 通知里两个选项都编号 `001` | 稀 | E-23 | 该事件标为选项编号冲突。**XSD 与通知侧约束都沉默**，C88 只是建议 | — |
| `CHOS` 事件两个选项都 `DfltOptnInd=true` | 稀 | E-24 | 标为默认不唯一，沉默账户的结果不确定 | — |
| `CHOS` 事件零个默认选项 | 稀 | E-24 | 同上的反方向：沉默账户无结果可给 | — |
| `CHOS` 事件不给 `RspnDdln` | 中 | E-25 | 标为无法判定迟到。**规范允许** | — |
| 截止前被接受选 `001`，确认却是 `002` | 中 | E-26 | 标为执行与指令不符。**这是"客户说选现金、实际收到股票"的那条投诉** | — |
| 沉默账户确认为 `001`（非默认），且无 `DfltActn` | 中 | E-27 | 标为默认未执行 | — |
| 选证券账户按事件级 F 而非选项比率调整 | 稀 | E-28 | 标为把 §7 第 3 条套在整个事件上 | — |
| `ADEA` 拒绝的指令时点早于 `RspnDdln` | 稀 | E-29 | 标为拒绝码与时点矛盾 | — |
| 指令 `OptnTp=TAXI`，通知同编号为 `CASH`，确认 `OptnNb` 一致 | 稀 | **无人捕获，这是预期** | **E-30 的配对注入**：`TAXI` 不在通知码集里。**若被报出来，说明判据在比类型不在比编号** | — |
| 沉默账户一条 `DfltActn` 状态回报，不带 `InstrId` | 中 | **无人捕获，这是预期** | **E-31 的配对注入**。**若被报出来，说明 `InstrId` 被当成必填** | — |
| 丙账户迟到被拒 `ADEA`，随后 `DfltActn`，确认为默认选项 | 中 | **无人捕获，这是预期** | **E-27 与 E-29 的联合配对**：迟到后执行默认是正常路径。**若 E-26 报出来，说明被拒指令被当成了被接受** | — |
| 重发指令带 `Lnkgs`，`Ref` 走 `SctiesSttlmTxId`，`PrcgPos` 为 `INFO` | 中 | **无人捕获，这是预期** | **E-18 与 E-19 的配对注入**：这是重发的唯一正确写法。**若这条被报出来，说明判据把"有链接"本身当成了问题** | — |
| 一条 `seev.037` 冲正过账，事件本身仍确认且调整因子不动 | 中 | **无人捕获，这是预期** | **E-15 与 E-9 的配对注入**：这是冲正的正常样子。**若这条被报出来，说明处理路径仍然合并着**，与 §6.4A 上面那条最贵错误互为正反面 | — |
| 失败报文时点提前到结算日结束之前 | 稀 | V3-2 | 同上。**这是主 BO 要证明的那条判定的反面** | — |
| 第 3 步与第 4 步用不同原因码 | 稀 | V3-1 | 同上 | — |
| `FctvSttlmDt` 早于 `SttlmDt` | 稀 | V1-025-5 | 同上。**提前结算不在任何已冻结场景内** | — |
| 参与方填一个形状合法的 BIC | 稀 | V1-023-10 | 同上。**XSD 只检查形状不检查注册**，见[生成规范](data-generation-specification.md) §3B.5 | — |

**"失败报文时点提前"那一条的地位与其他不同。** 它是
[业务目标](business-objectives.md) §3.1 的 T+1 早晨异常处理流要证明的那条判定的反面：
**挂起与失败共用同一批原因码，区分它们的只有结算日**。**这条注入不通过，整个主 BO 的
说法就没有立住。** 其余注入证明的是某条检查有效，这一条证明的是主张成立。

### 6.5 资金段的注入

**§2C.9 已定的三条在此并入。** 本段注入的特点是**必须成对设计**，
因为组头违规与交易行违规的爆炸半径不同，见 §2C.2。

| 注入 | 档 | 预期捕获者 | 正确处理后的预期结果 | 失效于 |
|---|---|---|---|---|
| `NbOfTxs` 与实际交易行数差一 | 稀 | C1-2 | **整组标为契约违规，N 笔全部不进正常对账** | — |
| 某条 `UndrlygAllcn/Amt` 改小，组内不配平 | 稀 | C2-6 | **仅该笔作废，同组其余交易不受影响** | — |
| `CtrlSum` 与各 `IntrBkSttlmAmt` 之和不等 | 稀 | C1-3 | 整组作废 | — |
| 组头与交易层**同时**填 `IntrBkSttlmDt` | 稀 | C2-4 | 整组标为契约违规。**这是异或被违反的一半** | — |
| 组头与交易层**都不**填 `IntrBkSttlmDt` | 稀 | C1-6 | 整组标为契约违规。**这是异或被违反的另一半，两条必须同时存在** | — |
| `SttlmMtd=CLRG` 的报文带一个 `SttlmAcct` | 稀 | C1-7 | 整组标为契约违规 | — |
| `Rsn/Cd` 填 `NARR` 且不带 `AddtlInf` | 稀 | C2-13a | 该状态回报作废。**规范在此要求自由文本，而 CMOP 不生成自由文本，唯一出路是不用这个码** | — |
| 一条不带 `CtrlSum` 的合法报文 | 中 | **无人捕获，这是预期** | C1-3 带条件，**此注入证明它没有被写成无条件不变量** | — |
| `pacs.002` 不带 `OrgnlEndToEndId` | 中 | C2-9 | 该状态回报标为孤儿，不进链条 | — |
| `TxSts` 填一个已废止的码 | 稀 | C2-11 | 该笔作废。**废止码在 XSD 里是普通枚举值**，见契约 §4A.9 | — |
| `BkTxCd` 三层各自合法但组合不存在 | 稀 | C2-17a | 该明细标为契约违规 | — |
| 具体子族码配一个泛化族码 | 稀 | C2-17 | 该明细标为契约违规。**这是规范那半条，与上一条分开注入** | — |
| 贷方明细也填 `PMNT/ICDT/FICT` | 中 | C2-17c | 该明细标为方向与族码不符。**组合表校验必须保持沉默**，它是合法组合 | — |
| 一条 `pacs.002` 在 `ACCC` 之后再发 `ACTC` | 中 | C3-5 | 该链标为次序违规 | **P-3 解锁后删除**，退汇与冲正使状态回退合法 |
| 一条链完全不发 `pacs.002` | 中 | C3-3 | **标为 CMOP 生成缺陷，不标为规范违规** | — |
| `UndrlygAllcn/RltdRefs/SctiesSttlmTxId` 指向不存在的 `TxId` | 中 | C4-1 | 六段链条判定为未闭合 | — |
| S-2 的券腿失败，但款腿照发 `ACCC` | 稀 | C4-5 | 该笔标为两腿脱钩，**券款双方都退出正常对账** | — |
| P-2 的 `FctvIntrBkSttlmDt` 取预定日而非券腿实际日 | 稀 | **C4-6，且 C4-2 必须保持沉默** | 该笔标为实际结算日不一致 | — |
| P-2 的 `IntrBkSttlmDt` 被改写成实际延迟日 | 稀 | C4-2 | 该笔标为指示日被事后重写 | — |
| `Btch/NbOfTxs` 写二十但只给十九条 `TxDtls` | 稀 | C4-11 | 该明细作废，**同一对账单其余明细不受影响** | — |
| 一条 `TxDtls/Refs/EndToEndId` 指向当日没有的支付 | 中 | C4-10 | 该笔标为对账单侧多出，**不标为资金段缺失** | — |
| 某笔已结算支付在当日对账单里完全没有对应 `TxDtls` | 中 | C4-12 | 该笔标为对账单侧漏记 | — |
| 一条 `pacs.004` 退汇，券腿没有对应的 `sese.026` | 中 | C4-14 | 该笔标为两腿在反向上不对应。**在 DVP 下等于凭空少一笔钱** | — |
| `pacs.004` 带上 `UndrlygFICdtTrf` | 稀 | C2-25 | 该退汇标为契约违规。**这条注入证明的是"能填不等于该填"** | — |
| `GrpRtr=false` 但一条 `TxInf` 都不带 | 稀 | C1-9 | 整组作废 | — |
| `GrpRtr=true` 的整组退汇，一条 `TxInf` 都不带 | 中 | **无人捕获，这是预期** | **C4-16 的配对注入**：这是整组退汇的合法样子。**期望结果是逐笔判据全部跳过而批级判据照常运行**；若逐笔判据报"定位不到笔"，说明退化被当成了错误 | — |
| `CLBD` 改动一分钱 | 稀 | C4-8 | **整张对账单不自洽**，在资金段数据到齐前即判定 | — |
| `camt.052` 里放一条 `Sts/Cd=PDNG` 的明细 | 中 | **无人捕获，这是预期** | **C4-18 的配对注入**：日内报告带未入账明细正是它的 scope。**若这条被报出来，说明 C4-18 被写成了按明细判而不是按报文类型判** | — |
| `camt.053` 里放一条 `Sts/Cd=PDNG` 的明细 | 稀 | C4-18 | 该对账单标为超出报文 scope。**XSD 与 31 条约束全部会保持沉默**，见契约 §4B.12 | — |
| 日内报告的一条 `PDNG` 明细在当日对账单里没有对应 `BOOK` | 中 | C4-17 | 该笔标为未入账悬挂，**不标为对账单漏记**，两者的责任方不同 | — |
| 一条 `PDNG` 明细在对账单里消失，且没有任何 `pacs.002` | 中 | C4-26、C3-3 | **两条都应报**：一条说消失无理由，一条说状态回报漏发 | — |
| 一条 `PDNG` 明细在对账单里消失，且有配套的 `RJCT` | 中 | **无人捕获，这是预期** | **此注入证明 C4-17 的适用条件生效**，合法的消失必须沉默 | — |
| 被拒的那一笔仍以 `BOOK` 出现在对账单 | 稀 | C4-27 | 标为已拒却入账。**这是本段最贵的一条**，钱没到而账上有 | — |
| 被拒的那一笔以 `RvslInd=true` 出现 | 稀 | C4-28 | 标为冲正了一笔从未入账的明细 | — |
| 被拒之后换新 `EndToEndId` 重发，新的那笔以 `BOOK` 入账 | 中 | **无人捕获，这是预期** | **C4-27 的配对注入**：重发是被拒之后的正常做法。**判据必须按被拒的那个引用判，不能按底层这笔支付判**；若报错，说明锚点取错了一层 | — |
| 一笔先 `BOOK` 后被真冲正，明细带 `RvslInd=true` | 中 | **无人捕获，这是预期** | **C4-28 的配对注入**：冲正本身完全合法。**判据只在存在 `RJCT` 且此前没有 `BOOK` 时才成立**；若报错，说明条件被省成了"见 `RvslInd` 就报" | — |
| `RJCT` 的时点早于日内报告 | 稀 | C4-29 | 标为次序违规。**这一笔本来就不该报成 `PDNG`** | — |
| 明细只给到 `NtryDtls`，不给 `TxDtls` | 中 | C4-19 | 该明细标为锚点缺层。**C4-10 必须保持沉默**，它比的是命中，不是存在 | — |
| `TxDtls` 给了但 `Refs` 整块不给 | 中 | C4-19 | 同上，**两条注入证明 C4-19 是逐层判定而不是只判最后一层** | — |
| 未入账明细的 `BookgDt` 与入账后的 `BookgDt` 相差两个工作日 | 中 | **无人捕获，这是预期** | **此注入证明 C4-20 生效**，预测误差是产出不是错误 | — |
| 一条明细同时带 `Avlbty` 与 `ValDt` | 稀 | C4-21 | 该明细标为契约违规。**期望结果是只有 CMOP 判据报错**，XSD 与约束均沉默 | — |
| 冲正明细按被冲正那条的方向填 `CdtDbtInd` | 稀 | C4-8 | **整张对账单不自洽**，因为有符号求和差两倍金额。见契约 §4B.12 冲正段 | — |
| 内部账未入账金额与 `PDNG` 明细之和差一笔 | 中 | C4-22 | 标为三方对账不平，**并按 C4-17 定位到具体是哪一笔** | — |
| 删掉中间一个营业日的 `camt.053` | 稀 | **C4-41 与 C4-39** | **两条都应报**：一条说少了一份报文，一条说次日期初对不上。**只报后者说明缺日被当成了余额错误** | — |
| 次日 `OPBD` 比前日 `CLBD` 多一分钱 | 稀 | C4-39 | 标为跨日断裂。**两份报文各自的 C4-8 都保持沉默**，这正是跨日判据存在的理由 | — |
| `PRCD` 与 `OPBD` 不等 | 稀 | C4-40 | 该报文作废 | — |
| `XPCD` 按 `ITBD` 原值填，漏掉待处理项 | 中 | C4-42 | 标为预测余额口径错误。**内部账那条算路保持平衡**，两条算路不一致即为信号 | — |
| 笔级状态回报用 `ACSC` 并据此记为已结算 | 稀 | V 词表层 | 标为把状态码当财务信息。**码值定义自带 warning**，契约 §4B.15.3 | — |
| `FUTR` 明细的 `ValDt` 等于其 `BookgDt` | 中 | C4-31 | 标为状态与日期不符。**那笔本该是 `BOOK`** | — |
| `FUTR` 转 `BOOK` 时把 `BookgDt` 改成起息日 | 中 | C4-32 | 标为入账日被事后重写。**同一改动在 `PDNG` 下是合法的**，见 C4-20 | — |
| 一笔 `FUTR` 到了 `ValDt` 当日仍是 `FUTR` | 中 | C4-33 | 标为起息未落地 | — |
| 当日 `CLBD` 减 `OPBD` 把 `FUTR` 金额也算进去 | 中 | C4-34 | 标为余额口径错误。**C4-8 的原式在这一天必须停用** | — |
| `FUTR` 明细带 `RvslInd=true` | 稀 | C4-35 | 标为规范违规。**依据是码值定义，不是约束**，XSD 会保持沉默 | — |
| 一笔在 `ValDt` 之前由 `FUTR` 转 `PDNG` | 稀 | C4-36 | 标为账上退回账下。**这一条是 CMOP，对外不能说成规范违规** | — |
| 一条 `INFO` 分录进了内部账 | 稀 | C4-38 | 标为提示类分录被记账 | — |
| 当日全是 `BOOK`，`CLBD` 减 `OPBD` 用原式求和 | 中 | **无人捕获，这是预期** | **C4-34 的配对注入**：不含 `FUTR` 的日子原式必须继续生效。**若这条被报出来，说明限定版把原式顶掉了** | — |
| 一笔 `BOOK` 明细带 `RvslInd=true` | 中 | **无人捕获，这是预期** | **C4-35 的配对注入**：`BOOK` 是唯一可冲正的状态。**这与 C4-28 的配对注入是同一条数据的两种用法** | — |

**前两条是本段设计的核心，必须同时存在。** 一条注入证明检查有效，
**两条配对才证明分级有效**：第一条应当作废 N 笔，第二条应当只作废一笔。
**若两者的处理结果相同，说明 C1 与 C2 分级形同虚设**，而那是 §2C.2 全部结构的前提。

**"完全不发状态回报"那一条的预期结果特意与众不同。** 规范说状态回报是可选的
（契约 §4C.4），**所以这条数据在外部世界是合规的**，它只违反 CMOP 自己的生成约定。
**把它标成规范违规是本层越界**，而这种越界在对外口径里会变成一句错话。

### 6.6 三条贯穿全部四段的注入

**它们不针对任何单条断言，针对的是管道本身。**

| 注入 | 档 | 预期捕获者 | 正确处理后的预期结果 |
|---|---|---|---|
| 同一批次投递两次 | 密 | 幂等断言 | **去重后结果与投递一次完全相同**，包括行数与快照内容 |
| 某一段整批缺失 | 密 | 缺失类别与门禁 | 门禁默认失败，**不得因"下游没报错"而放行** |
| 词表版本在两次运行之间变化 | 稀 | §2C.7 的版本标识 | 两次运行的结论可以不同，**但每次都必须能说出自己用的是哪个版本** |

**第三条是本轮新增的。** 它在四段里只对资金段有实质影响，但**放在贯穿类而不是资金段**，
因为它检验的是"结论是否携带产生条件"这件事，与 §5 里 FINTRAC 规则版本、
与重跑口径是同一个问题。**一次结论说不出自己的前提，就不能作为证据使用。**

## 7. 未决项

- 各比对对的容差数字与推导。
- 各门禁的阈值，需要 Phase 1 实测后确定，不能提前拍。
- 故障注入的具体比例。
- 异常队列的排序函数：金额、结算截止时点与账龄如何加权。
- 外部对账（R3、R4）的对手方数据到达延迟假设。
- ~~结算段业务校验的完整故障注入清单。~~ **已于 2026-09-12 补齐，见 §6.4。**
- ~~分配与确认段的故障注入清单。~~ **已于 2026-09-12 补齐，见 §6.3。**
- **注入比例的三档数值是占位值**，见 §6.2。档位划分与理由已定，数值待 Phase 1 实测。
- ~~资金段（`pacs`、`camt`）的业务校验层尚未写。~~ **已于 2026-09-12 写成，见 §2C。**
  核实结果是**不能照搬**：批量结构迫使单报文一级拆成组头与交易行两级，词表外置引入了
  一整类带版本的检查，而 `camt` 的一条明细可以覆盖多笔支付，**按条数对账在规范上就是错的**。
- ~~资金段跨段检查 C4-5 现在无被测数据。~~ **已于 2026-09-12 解除**：P-2 冻结后
  它有了被测数据，同时新增 C4-6 与 C4-7，见 §2C.6。
- **起息日与入账日不同的情形未覆盖。** P-4 一律令两者相等（契约 §4B.9），
  **真实对账单里两者可以差若干天**，这类差异属于另一个场景。
- **未入账明细（`PDNG`、`FUTR`）与内部账的对账未写。** 已登记为场景 P-5。
- **词表升版的重跑口径未定。** §2C.7 判定词表升版是一次需要重跑的事件，
  但重跑范围是全量还是仅受影响码值所在的分区，未定。
- ~~**部分结算确认的次序检查尚未写。**~~ **2026-09-13 写成 V2-15**，随 S-9 冻结。原文：出处已有（`PartialSettlementGuideline`，`PAIN` 在前、
  `PARC` 收尾，见[契约](regulatory-data-contracts.md) §4C.3），但 S-1 与 S-2 都不产生部分结算，
  **本轮写了也无法用注入证明它有效**，随场景解锁一起补。
- ~~其余报文的具名约束尚未逐条过。~~ **`sese.026` 至 `sese.028` 已于 2026-09-12 取全清单逐条读**，
  见契约 §4C.6。收获是三条跨报文约束（V1-ALL-1 至 V1-ALL-3）与一次推翻（V1-025-4 改判为规范）。
- **按约束名检索得出的"没有这条约束"结论都不可信。** 同一条业务规则在不同报文上
  跟着元素名改名，见契约 §4C.6.1。**§4C.1 至 §4C.5 里此类结论应逐条重验**，本轮只重验了金额一条。
