# 代理协作规范

本文件面向在本仓库工作的 AI 代理，也适用于人类协作者。它只做路由，具体规则在各自的
文档里，**这里不复制它们的内容**。

## 先读哪一篇

| 任务 | 先读 |
|---|---|
| 往仓库里写文档 | [docs/README.md](docs/README.md)，再看 [docs/dev/README.md](docs/dev/README.md) 的路由顺序 |
| 需要判断某段内容该写哪边 | [ToucanShelf 协作约定](docs/dev/collaboration.md) |
| 用户明确要求写 `toucan-notes/` | 该目录自己的 `AGENTS.md` 与 `.memogit/skill/SKILL.md`，**动手前必读** |

一句话版本：**`docs/` 放结论与约束，`toucan-notes/` 放素材与过程。**
笔记不属于本仓库，被 gitignore，不会被发布；仓库文档永远不引用笔记。

**`toucan-notes/` 以人读人写为主：代理可以自由读，只在用户明确指令时才写。**
路由判断落到笔记一侧时，代理不主动写入，而是在对话里告诉用户这段内容适合进笔记的哪里，
由用户决定是否写、谁来写。

<!-- handoff-trial:begin -->
## 独立执行队列（2026-09-14 起试行）

协作中识别出适合人离开后独立完成的任务，挂起到 [TODO.md](TODO.md) 的 Handoff Queue，
不当场做。队列格式、合同模板和下线标准都在那一节。

- **准入**：同时满足四条才提议挂起。一是不能一两分钟内做完；二是输入已冻结，挂几个小时
  不会变；三是当前协作接下来要做的事用不到它的结果；四是写得出合同，尤其是可检查的验收标准。
  写不出合同就不是这类任务。
- **判定权在人**：代理只提议「这个挂起可以吗」，用户同意才入队，不同意就当场做。
  需要人商定的决策、冻结场景、边界规则永远不入队。
- **交接**：用户说「收尾」或「handoff」时，逐条核对输入是否仍是合同里的版本，变了就标
  stale 不跑；报一遍要跑哪几条、预计产出什么，用户确认后再执行。不因会话结束自动开跑。
- **执行**：每条在独立分支或 worktree 里做，产出草稿 PR，不合并。遇到歧义不猜，
  记录问题、跳过该部分、继续。
- **回收**：用户回来时先给一份审阅清单：完成、部分完成、跳过及原因、积累的问题。
  每条补上合同里的 outcome 行，这是每周复盘和决定是否下线的唯一依据。
- **足迹**：试行期的所有落点都登记在 TODO.md 该节的 Footprint 清单里。新增 skill、定时任务、
  脚本或其他文件前先登记，没登记的不许加。
<!-- handoff-trial:end -->

## 通用执行约定

- 只在用户要求时提交或推送。当前分支是 `main` 时先开分支。
- 文档中的数字、结论和失败归因必须留下最小可复核证据：日期、执行入口、数据版本。
- 探测脚本与临时输出不进仓库，放会话临时目录。**例外是产出被文档引用的参考数据的抽取
  脚本**：它是那些数字的出处，必须与产物一起提交，见 `tools/` 与 `data/reference/`。
  区分标准是产物有没有被引用，不是脚本长什么样。
- 新增、移动或重命名文档时，在同一次变更里更新父级索引和受影响的链接。
